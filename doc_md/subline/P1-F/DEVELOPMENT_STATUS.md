# P1-F 开发进度：发行后置与离线发布验收

> 最后更新：2026-04-20

## 当前阶段

- `P1-F` 已完成子线建档。
- `OsuGameDesktop.CreateStorage()` 已按 `portable.ini -> data/` 接通便携数据根；Release publish 的便携模式与基础覆盖更新路径已有已验证结论。
- 游戏内在线更新仍保持禁用，当前重点不是恢复安装器/在线更新，而是继续收口公开发行物产品面与最终 release gate。

## 进度矩阵

| 事项 | 状态 | 备注 |
| --- | --- | --- |
| 子线建档 | 已完成 | 四件套已建立 |
| 便携发布基线 | 已完成 | `portable.ini -> data/` 与覆盖更新路径已有验证结论 |
| 在线更新关闭基线 | 已完成 | 当前仍维持离线优先，不恢复在线更新 |
| 公开发行物产品面验收 | 进行中 | 依赖 `P1-A` 的默认皮肤与 release gate 收尾 |
| 发布口径同步 | 进行中 | 需持续联动 `../../other/RELEASE.md` |

## 验证记录

- 沿用既有验证结论：Release publish 后 `portable.ini` 会触发 `data/` 自动生成；本轮仅同步代码现状，未新增构建或测试命令。