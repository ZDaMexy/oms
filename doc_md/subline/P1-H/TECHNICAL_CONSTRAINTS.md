# P1-H 技术约束：存储拓扑支撑线

1. 不得破坏 `chartbms/`、`chartmania/`、`portable.ini -> data/` 与本地优先的数据根约束。
2. 任何改变导入路径、数据根、外部谱库扫描或重扫策略的改动，都必须同步更新本目录四件套、`../../mainline/` 与 `../../other/RELEASE.md`。
3. 不得让存储拓扑回退为需要重新导入或依赖在线迁移的模型。