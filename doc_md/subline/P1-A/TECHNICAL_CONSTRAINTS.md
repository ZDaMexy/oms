# P1-A 技术约束：Skin V1、产品面与 release gate

> 最后更新：2026-08-24
> 本文件是 Skin V1 的硬约束源。执行顺序见 [DEVELOPMENT_PLAN.md](DEVELOPMENT_PLAN.md)，当前事实见 [DEVELOPMENT_STATUS.md](DEVELOPMENT_STATUS.md)，设计证据见 [SKIN_SYSTEM_V1_ARCHITECTURE_20260710.md](../../other/SKIN_SYSTEM_V1_ARCHITECTURE_20260710.md)。若代码与本文冲突，先确认新事实并同步修正文档/代码，不能用历史 CHANGELOG 覆盖当前 authority。

## 归线与产品边界

1. P1-A 拥有 shared skin package/runtime/fallback、BMS playfield/BGA skin boundary、G1 和 release gate；判定/反馈、输入、BGA 时间线、存储底层语义仍分别归 P1-C/P1-E、P1-B/P1-D、P1-L、P1-H。
2. 不得借 Skin V1 提前带入完整 FHS、dan、1P/2P binding flip、BSS/MSS、联网或其它 Phase 2/3 功能。5K/7K 的 P1/P2/center style 是视觉 lane order/停靠，不是 binding flip。
3. `osu.Game` 不得新增对 `osu.Game.Rulesets.Bms` 的编译期依赖。共享合同只能包含 ruleset-neutral DTO/runtime；BMS/mania 具体适配留在各自 ruleset。
4. BMS→mania 公开入口、映射语义和 Skin V1 是不同专题。P1-A 只拥有入口/文案/presentation gate，source keymode→mania mapping 仍归 P1-K。
5. 首次启动向导、settings trim 等既有产品面继续遵守 desktop-only 装配、OMS localisation 和模块缺失优雅退化约束；它们不因 Skin V1 重写。
6. 新增shared slot/topology/config/event/capability/candidate合同必须在同一切片或紧随切片指定真实production host、renderer或authoring consumer；无consumer的DTO/fixture不得作为产品进度，也不得继续横向扩张来替代端到端能力。

## 既有产品面硬约束（未被 Skin V1 取代）

1. 当前 `GN / WN` 只能表述为 OMS 当前 `Normal / Floating / Classic Hi-Speed + Sudden / Hidden / Lift` runtime surface 的反馈；`Floating` 只锚定 initial BPM，不得对外宣称完整 mid-song re-float、soflan GN range 或 IIDX `FHS`。
2. settings 可显示 Hi-Speed 模式、当前模式值和未启用 `Sudden / Hidden / Lift` 时的基础下落时间；不得在 settings 显示 runtime-adjusted `GreenNumber`/可见毫秒或暗示完整 BPM 补偿/FHS。
3. `Lift` 是 geometry control，`Hidden` 是下遮挡；命名、状态、HUD 与 pre-start overlay 不得混写。
4. Hi-Speed 范围保持 `Normal 1.0–20.0`、`Floating 0.5–10.0`、`Classic 0.5–10.0`；`GreenNumber = Round(VisibleLaneTime_ms × 0.6)`，`WhiteNumber = SuddenUnits`，`VisibleLaneUnits = 1000 - SuddenUnits - HiddenUnits`，Lift 只经几何间接影响可见时间。Classic 映射保持 `TimeRange = (100000 / 13) / HS`，`HS 10 + WN 350 => GN 300` 必须成立。
5. ruleset runtime geometry profile 继续冻结；`Playfield Scale` 固定 `1.0` 且不可配置。除 `Sudden/Hidden/Lift` 与 5K/7K `Playfield Style` 外，旧 playfield/receptor/bar-line config 不得作为用户可见 runtime contract 影响速度/几何。Skin package geometry 是独立、受 descriptor/合法域约束的作者面，不能借此恢复旧 runtime sliders。
6. `UI_PreStartHold` 继续承担“前 5 秒阻止开始 + 全程调速修饰键”；若 delay 在 hold 中耗尽，松开时必须重新调度完整 delay，不能立即开谱。`UI_LaneCoverFocus` 是 click-to-cycle 持久 target；`READY HOLD` 只用于阻止开谱窗口，`BMS speed` toast 在 hold 调速期间持续可见。
7. BMS mod/config 记忆必须 ruleset-local；不得隐式共享到 mania/全局 `SelectedMods`。局内 `Sudden/Hidden/Lift` 调整只在 `RememberGameplayChanges=true` 时回写所选 mod 与持久快照，关闭时保持 current-play-only。
8. 冷启动不得在 `RulesetConfigCache` ready 前调用 `GetConfigFor()` 构建 BMS mod persistence；先允许无 config 首轮 apply，再在 cache ready 后 replay 当前 ruleset restore。
9. 实现 `IPreserveSettingsWhenDisabled` 的 configurable BMS mod 停用只表示 inactive；除显式 reset/迁移外，不得清空最后配置。
10. `首次启动向导`、`Run setup wizard` 与无谱面引导归 P1-A；复用的存储/输入语义仍归 P1-H/P1-B。
11. 共享 onboarding 调用 BMS-only 能力时，`osu.Game` 不得直接引用 BMS ruleset；模块缺失须优雅退化。
12. 首次启动向导 OMS 文案必须使用 OMS-owned localisation namespace + `.resx`；只改 `*Strings.cs` fallback 不足以覆盖非英文资源。
13. desktop settings 隐藏 upstream tablet/touch/mouse subsection 只能在 `OsuGameDesktop` 等 desktop host 完成，不得下移为 `OsuGameBase` 全宿主行为。
14. mania settings 的滚动速度毫秒只代表标准车道几何参考下落时间，不是跨皮肤/跨 ruleset 体感合同。
15. `BMS -> mania` 公开口径只能是 `BMS source -> mania target`，不能暗示 generic convert；P1-A 只拥有入口/文案/presentation/unavailable feedback，mapping/flatten/scratch 退化/空结果仍归 P1-K/K9。
16. unsupported/invalid BMS→mania case 必须隐藏或明确不可用，不能显示可点击空壳。
17. playfield 顶边默认贴屏幕顶边；当前默认 `PlayfieldHeight=0.92`，`HitTargetVerticalOffset=0`，保持 `scrollLengthRatio == 1`、TimeRange/GN/判定窗口不受场高改变。不得用整体下移或 HUD safe-area inset 破坏该合同；改变 descriptor 时须同步 lane/layout/GN 测试。

## 核心 ownership

### 引擎必须拥有

1. 谱面、输入、判定、计分、gauge、scroll/STOP/gimmick、BGA timeline/seek/POOR 的真实状态和时钟。
2. lane topology/order/action、playfield group/bounds、judgement position、BGA safe viewport 和 gameplay clock。
3. 外部 package 的资源解析、权限隔离、版本协商、预算、错误熔断、fallback 与原子 reload。
4. 对皮肤发布不可变、版本化、只读的 layout/state/event DTO。

### 外部皮肤可以拥有

1. slot 内的 sprite/container/text/mask、素材、颜色、混合、裁剪和标准视觉效果。
2. 帧动画、tween、timeline、状态机和对只读 gameplay event 的表现响应。
3. 可选组件是否显示，以及在 descriptor slot 内的局部布局/装饰。
4. 可选沙箱脚本，但只能调用获准的 scene/animation API。

### 外部皮肤不得拥有

1. lane order/action、判定线时序、scroll transform、BGA playback clock 或任何 gameplay mutation。
2. 输入注入、判定/计分/gauge 写入、谱面/Realm/配置写入。
3. 网络、任意文件系统、反射、进程、线程、原生库和未声明宿主能力。

## 共享与分离约束

1. mania/BMS 共同的 ini tokenizer、颜色/数值/数组、资源路径、帧序列、诊断、scene node、animation/state-machine、event envelope、fallback 和 sandbox 必须共享实现，不得继续维护两套近似 codec。
2. 共享层只能认识 `LaneGroupId`、`LaneId`、`LaneRole`、`Side`、bounds、capability 和 stable event DTO；不得出现 `BmsKeymode`、`ManiaAction`、`BmsGaugeProcessor`、`DrawableRuleset` 等具体类型。
2a. shared identity 使用强类型 `GameplaySkinLaneGroupId` / `GameplaySkinLaneId`，值必须是非敏感的小写 ASCII 点分 opaque topology token 并按 ordinal 比较，不得嵌入用户、包、资源名或路径信息。GroupId 在单一 topology 内不得分配给两个不同 semantic group，LaneId 跨该 topology 全部 group 不得分配给两个不同 semantic lane；同一语义实体可跨不改变 topology 的 revision 重建 identity，但必须复用相同 ID。consumer 不得从 visual order、geometry、本地化文本或 ruleset CLR enum 反推 ID，也不得把进程内 hash 持久化。
2b. stable ID 跨 P1/P2/center presentation、视觉重排、geometry、skin reload 和不改变 topology 的 layout revision 保持不变；同 LaneId 的 group membership 与 role 不得漂移。跨这类 revision 关联比较 `Id`，而 group/lane identity 的完整值相等包含当前 neutral metadata；`GameplaySkinLaneTopologySnapshot` 负责拒绝单 snapshot 内的重复 ID 或 membership metadata 冲突，transition validator 只验证调用方已声明为 topology-preserving 的两个 neutral snapshot，不替代 native context/revision authority。
2c. `GameplaySkinLaneRole` 固定为 `Key/SpecialKey/Scratch`（另有必须拒绝的默认 `Unspecified`）。mania internal projection 必须将 odd-stage 的 stage-local centre 映为 `SpecialKey`，但它仍是 key input、绝不能映为 `Scratch`；note/LN/mine 是对象类型，不是 lane role。
2d. `GameplaySkinLaneSide` 固定为 `Neutral/Primary/Secondary`（另有必须拒绝的默认 `Unspecified`），表示逻辑 player/deck presentation side，绝不等同屏幕 Left/Right、BGA side 或 input binding owner。5K/7K P1/CenterP1 为 Primary、P2/CenterP2 为 Secondary，9K BMS/PMS 为 Neutral，14K 两 deck 分别 Primary/Secondary；style 改变 side 时 stable ID/action 不变。
2e. identity primitive 不携带 index。`GameplaySkinLaneTopologyEntry` 显式承载零基 `GlobalLogicalIndex`、`GroupLocalLogicalIndex`、`GlobalVisualIndex`、`GroupLocalVisualIndex`；group 承载零基 logical/visual group index；snapshot 提供 immutable logical/visual 有序视图与强类型 ID lookup。该 snapshot 故意不携带 keymode/style、action/source channel、bounds/rect、geometry、revision 或 native context，也不是完整 `GameplaySkinLayoutContext`、manifest/event/JSON ABI。
2f. topology snapshot/group 必须非空且防御性复制输入；拒绝 null、负 index、重复 GroupId/LaneId、lane 与 containing group identity 不一致、非 `0..count-1` permutation、group-local 与对应 global order 不一致，以及 group 在全局 logical 或 visual 序列中非连续块。所有 index 均为当前 snapshot 的 0-based order 数据，不得升级成 stable identity。
2g. BMS internal projection 只能以 `BmsLaneLayout` 为 authority：`Lanes` 仍按 logical `LaneIndex` 存储，resolved left-to-right order 只读 `Lane.VisualIndex`，不得从枚举位置或 geometry 反推。canonical token 固定为 `bms.group.deck-1/2`、`bms.lane.scratch-1/2`、`bms.lane.key-1..14`；5K/7K 为 `S1,K1..`，9K BMS/PMS 为 `K1..K9`，14K 为 `S1,K1..K14,S2`。unknown enum、非 canonical lane count 或 action/scratch composition 必须拒绝。
2h. BMS 5K/7K 的 P1/Center group side 为 Primary、P2/CenterRightScratch 为 Secondary；皿左右重排只通过 visual index 表达，不得改变 logical index、action 或 binding authority。9K BMS/PMS 的 neutral topology 相同、side 为 Neutral、applied style 为 Center，但 internal native keymode 必须保持区分；14K 为两个各 8-lane 的 Primary/Secondary group，S1 global/local=`0/0`、S2=`15/7`。
2i. topology-preserving validator 必须要求前后 GroupId/LaneId 集合完全相同、每个 group 的 logical index 稳定，并要求每个 lane 的 group ID、role、global logical index 与 group-local logical index 稳定；group side、group visual index 与 lane global/group-local visual index 可变。不得比较完整 group/lane identity equality，也不得把 9K BMS/PMS 等相同 neutral shape 的通过写成 native keymode、完整 `GameplaySkinLayoutContext`、revision 或 wire transition 已验证。
2j. mania internal projection 先防御性复制 stage authority，只接受 1–2 stage、每 stage 1–`ManiaRuleset.MAX_STAGE_KEYS`；single group side 为 Neutral，dual stage 0/1 为 Primary/Secondary。token 固定为 `mania.group.stage-1/2` 与全局前缀序 `mania.lane.column-N`；global index 用 stage column count 前缀和，group-local index 用 stage-local ordinal，当前 visual=logical；odd-stage special 判定必须使用 stage-local index。
2k. `GameplaySkinLaneTopologyPublication` 只发布一个 immutable neutral topology 与 process-local revision；首个成功值为 0，后续成功值 checked `+1`。owner 必须先通过 exact immutable/non-sensitive native-context comparator，再通过 neutral transition validator 与 overflow 检查，全部成功后才替换 `Current`；mismatch、comparer 异常、neutral rejection 与 overflow 均不得推进。它不是 package revision、event `layoutRevision`、serialisation/wire ABI、thread-safe owner 或 security boundary。
2l. ruleset native continuity 留在 internal adapter：BMS owner 以 exact `BmsKeymode` 为 authority，`AppliedStyle` 只算 presentation metadata，可在 neutral transition 允许时改变；9K BMS/PMS 即使 neutral validator 接受也必须由 native gate 拒绝。mania owner 以 exact ordered stage-column vector 为 authority，4→5、stage count 变化与 `[4,5]→[5,4]` 必须拒绝；projection 不接受外部 topology 参数，只能从已复制向量生成 canonical topology。ruleset wrapper 不得上升为 shared CLR 或作者 ABI。
3. mania adapter 拥有 stage/column、legacy 480 坐标、mania action/result 映射；BMS adapter 拥有 scratch/DP、lane cover、gauge、BGA、STOP/scroll/gimmick 映射。
4. BMS 不得直接继承/复用 `ManiaLegacySkinTransformer`、`Column` 或 mania Drawable 作为生产架构。可以复用其 fixture、共同 codec 和 neutral runtime。
5. 新动态视觉优先由通用 event/scene 能力表达。在 ABI 缺口经 fixture 证明前，不得继续按“一件效果一个 `DefaultBmsXxxDisplay` + 私有 interface”扩张。

## ini 兼容约束

1. `[Mania]` 现有语法与素材包必须兼容；抽 shared codec 时不得无迁移改变既有用户皮肤行为。
2. BMS 与 mania 重合的字段使用相同键名、值类型、数组/颜色语义、资源名和 `name-0`/`name-1` 帧序列。`[Bms]` 只定义 scratch/side/DP/gauge/BGA/gimmick 等真正独有字段。
3. `[Bms]` 共同字段和 `[Mania]` 必须进入同一个 neutral configuration model，不得只复制 enum/key 名。
4. BMS compatibility 优先级固定为：5K `[Bms] → Keys:6 → Keys:5 → canonical marker`；7K `[Bms] → Keys:8 → Keys:7 → marker`；9K BMS/PMS `[Bms] → Keys:9 → marker`，不得重复制造同一 `Keys:9` key-only candidate；14K `[Bms] → Keys:16 → 同一个真实 Keys:8 bucket 按两个 engine-owned deck 分别投影 → Keys:14 普通键 → marker`。14K deck-local `Keys:8` 必须先于 `Keys:14`，以保留双 scratch 与 deck-local presentation；当前末端 marker 只能是 `Absent` 的未来 `oms-simple` authority 标记，不得伪造已装载 package。P2/CenterRightScratch 的 visual index 与 stable lane ID/action 必须由 fixture 固定，不能靠 renderer 猜。
5. mania 0-based column、当前 BMS `S/S2`/数字 token 必须在 adapter 边界转换成 stable lane ID；renderer/scene 不得继续自行拼 lane key。当前未版本化 `[Bms]` production lookup 对非 scratch 沿用 raw logical lane index：5K/7K/14K 因 scratch 占 index 0 而得到 `1..`，无 scratch 的 9K BMS/PMS 实际得到 `0..8`；这不改变 internal stable lane ID `K1..K9`。V1 canonical 作者 token 目标 `1..9` 必须经显式格式版本、迁移与冲突诊断引入，禁止同时静默接受 `0..8`/`1..9` 两套重叠别名。
6. BMS 现有 `PlayfieldWidth/Height`、normal/scratch width/spacing 等 F1 字段作为兼容输入保留，但最终映射须进入 engine layout descriptor，不允许脚本直接改时序。
7. `HitTargetVerticalOffset` 继续锁 0，直到独立专题证明 `scrollLengthRatio`、GN、判定窗口和 replay 不变量；不能通过皮肤偷偷改变。
8. 加载行为必须 fail-open 并产生结构化诊断：未知键、非法值、缺素材、不支持 capability 和 fallback 来源可查询。当前“静默跳过”只算恢复基线，不算 V1 完成。
9. neutral config 必须保留 explicit declaration/presence：presence carrier 的默认值是 `Absent`，显式 bucket 即使为空也为 `Declared`；declaration 只表示来源事实，不等于 slot `Provide`、验证成功或 `Suppress`。通用 carrier 必须区分显式 `false`、`0`、空字符串与缺失。legacy mania primitive scalar/indexed-array/known-global-colour/per-column-colour/`NoteBodyStyle` sidecar 必须在 decoder 成功转换、规范化、颜色解析或 enum 解析时同时捕获 presence 与 accepted value；exact known `[Mania] Keys:` bucket-global/non-column resource-name sidecar 与 mania/BMS 六类逐 lane resource sidecar 必须在 exact key 被 decoder 接受时同时捕获 accepted string。所有 sidecar 都不得从 synthetic/effective default 或之后可变的 native public 值反推；数组必须逐 index 保存，短数组未出现尾部为 `Absent`，空/invalid item 依既有兼容规则为 `Declared(0)`。现有 field-level config 只覆盖九个 primitive scalar、五组 indexed array、四个 exact known global colour、exact `Colour{n}`/`ColourLight{n}` 两类逐列颜色、十三个 exact bucket-global resource-name、note/LN head/body/tail/key up/down 六个逐 lane 资源字段、legacy mania `NoteBodyStyle`，以及 native `[Bms]` exact canonical 二十二项 colour 与十二项 geometry；任意扩展颜色、其它 arbitrary/prefix-only `ImageLookups`、native BMS 其余 resource 与完整 neutral config 仍未冻结。
10. shared codec 采用 adapter-first 迁移：mania presence 只能来自 `LegacyManiaSkinDecoder` 实际产出的 bucket，不能来自会为缺失 bucket 合成默认 configuration 的 `LegacySkin` lookup；BMS 只有 `BmsSkinDecoder` 接受并产出的有效 `Keymode` bucket 才算 `Declared`。当前冻结的source-fact面包括decoder-output lane-resource与候选计划、process-local provider/resolution/owner、primitive scalar/indexed-array/known global colour/per-column colour、exact bucket-global resource name、六类逐lane resource、legacy mania `NoteBodyStyle`及native `[Bms]` canonical colour/geometry sidecar与snapshot；它们不改变tokenizer、mania/BMS production lookup、renderer、`SkinManager`、真实fallback或reload。array snapshot保存converted compatibility values本身，不得提前把`ColumnSpacing`派生成左右间距或把explosion/light width派生成scale，也不得将boundary/gap source index冒充stable lane ID。known-global-colour snapshot只保存`ColourColumnLine`、`ColourJudgementLine`、`ColourBreak`、`ColourBarline`的parser-accepted `Color4`，不得提前doubled alpha、修正zero alpha、回落默认色或公开任意raw `Colour*` key/dictionary。`flushPendingLines()`异常前不清空坏行、malformed `Keys`沿用旧current config、duplicate `Keys`写入discarded config等既有行为不得被adapter迁移静默修复或误写成V1合同，须另立shared codec/malformed diagnostics决议。fixture稳定后再替换共同解析，不得把这些变更合并成一次生产切换。
10a. per-column colour 只接受 exact case-sensitive `Colour{n}` / `ColourLight{n}`，其中 `{n}` 是严格 1-based ASCII decimal token 且范围为 `1..Keys`；`01`、`+1`、大小写变化、`0`、越界、后缀或任意其它 `Colour*` 均不进入 sidecar。只有既有 decoder 成功接受并解析颜色后才能捕获当时的 parser `Color4`；sidecar 与公开副本都必须 defensive copy，不得从之后可变的 `CustomColours` 反推、伪造或擦除 declaration。
10b. lane-colour field catalog 仅包含 closed process-local 的 `LaneBackground` / `LaneLight`，不是 manifest/wire ABI。snapshot 必须绑定调用时同一个 exact immutable topology，防御性复制并按 logical lane/field 确定性排序；source-column 到 stable lane 的 ruleset projection 固定为 mania `GlobalLogicalIndex`、BMS full `GlobalVisualIndex`、14K deck `GroupLocalVisualIndex`（两个 deck 共享同一组 source index）、BMS key-only 的 non-scratch visual order。partial mapping 与多个 target lane 共享同一 source column 的 many-to-one mapping 合法；重复 target lane、target 不属于 exact topology 或 source 越界必须拒绝。当前 BMS full/deck/key-only 三个入口彼此独立且仅用于 fixture；若未来把多个投影合入同一 candidate plan，必须共享同一个 exact topology reference，禁止分别重建等价 snapshot 后混入同一计划。
10c. exact known `[Mania] Keys:` bucket-global/non-column resource-name 只包含区分大小写的 `LightingN`、`LightingL`、`StageLeft`、`StageRight`、`StageBottom`、`StageLight`、`StageHint`、`Hit0`、`Hit50`、`Hit100`、`Hit200`、`Hit300`、`Hit300g`。当前 source semantic mapping 固定为 `LightingN → ExplosionResource`、`LightingL → HoldNoteLightResource`、`StageLeft/StageRight/StageBottom → LeftStageResource/RightStageResource/BottomStageResource`、`StageLight → KeyFlashResource`、`StageHint → HitTargetResource`、`Hit0/Hit50/Hit100/Hit200/Hit300/Hit300g → MissJudgementResource/MehJudgementResource/OkJudgementResource/GoodJudgementResource/GreatJudgementResource/PerfectJudgementResource`；这些 mapping 只是 source-specific compatibility 语义，不是 neutral slot/manifest ID。exact key 仍必须写入既有 compatibility `ImageLookups`，同时保存 `SplitKeyVal` trim 后、尚未 `CleanFilename` 的 accepted string；显式 `Key:` 空值为 `Declared("")`，valid duplicate 取 last accepted。其它 broad-prefix `Hit*`/`Stage*`/`Lighting*` 可按旧 decoder 继续留在 mutable dictionary，但不得进入 closed sidecar。public snapshot 只能暴露固定十三属性，不得提供 raw string-key query/dictionary；decode 后 dictionary 的 add/replace/remove/reassign/clear 不得伪造、擦除或改变 sidecar。该 declaration 尚未经过 containment、文件存在/解码、动画帧、纹理预算、materialization 或 slot validation，不等于 `Provide`，也不是 topology、neutral resource catalog、manifest/wire ABI 或 production fallback。
10d. 六类逐 lane resource 的 decoder provenance 必须关闭 legacy mania 与 native `[Bms]` 两个 decoder→factory mutable window。legacy mania 只把区分大小写的 `NoteImage{n}`、`NoteImage{n}H/L/T`、`KeyImage{n}`、`KeyImage{n}D` 投入 sidecar，其中 `{n}` 是 `0..Keys-1` 的 canonical ASCII decimal，禁止 leading zero、符号、空 token、越界与后缀 lookalike；native `[Bms]` 只把既有 per-lane regex 接受后精确落入 note/LN head/body/tail/key up/down 六种 prefix+suffix 组合的声明投入 sidecar，并原样保存 regex 捕获的 `\d+`、`S` 或 `S2` raw token，禁止 numeric normalisation。故 9K factory 仍只查询当前 production-compatible ASCII `0..8`，14K 仍查询 `S`、ASCII `1..14`、`S2`；例如 raw `9`、`01` 或 Unicode decimal token 即使被 `[Bms]` decoder 接受并留在 sidecar，也不得静默别名到 9K stable lane。两侧 exact key 继续写入既有 public compatibility `ImageLookups`，显式空资源名保持 `Declared("")`，valid duplicate/重复 BMS bucket 保持 last accepted；其它 broad/prefix-only mania image key、BMS 错配 suffix、`LaneBackgroundImage*`、`LaneDividerImage*` 与 tokenless compatibility key 依旧遵守原 dictionary 行为，但不得进入 closed sidecar。两个 snapshot factory 只能读取 sidecar；decode 后对两侧 dictionary 内容的 add/overwrite/remove/clear、legacy dictionary 的整表 reassign 与手工 dictionary 注入均不得伪造、擦除或改变 declaration。internal accept/get 入口必须先拒绝 unknown/composite/non-canonical field、越界/非法 token 与 null resource，再同时写 compatibility view 与 sidecar，不得留下半写状态。该合同不执行 containment、文件存在/解码、动画帧、纹理预算、materialization 或 slot validation，不把 declaration 提升为 `Provide`，也不冻结任意其它 `ImageLookups` 为完整 V1 config。
10e. legacy mania `NoteBodyStyle` provenance 必须只在 exact、区分大小写的 source key 进入既有 `Enum.TryParse<LegacyNoteBodyStyle>(string, out ...)` 成功路径时捕获；该provenance合同不得追加 `Enum.IsDefined`、canonical numeric 或 flags 检查。命名值大小写敏感，而 undefined numeric（如 `1`、`99`、`-1`）、非 canonical numeric（如 `+2`、`02`）及逗号 composite 只要被现 decoder 接受，就必须原样保存其 parsed enum value；这只表示 compatibility parser 接受，不表示 V1 validated/author-supported style，后续 validation 必须另层诊断。缺 bucket 为 outer `Absent`，显式 bucket 但缺/坏字段为 inner `Absent`，valid duplicate 为 last accepted，后续 malformed value 不得擦除先前 accepted declaration；pending、malformed/duplicate `Keys` 等既有 bucket 行为保持。factory 只能读取 decoder-time sidecar；手工设置或 decode 后 erase/alter public `NoteBodyStyle` 字段不得伪造、擦除或改变 provenance。`LegacySkin` 根据全局 Version `< 2.5` 合成 `Stretch`、否则合成 `RepeatBottom` 的 effective default 永远不是 source declaration。该合同不得新增 native BMS field、改变任何 production consumer/fallback，或把 source-specific snapshot 提升为 neutral config、manifest/wire ABI。
10f. native `[Bms]` colour accepted provenance 的 closed set 固定为 exact、区分大小写的 `NoteColourWhite`、`NoteColourCyan`、`NoteColourYellow`、`NoteColourScratch`、`LaneBackgroundEvenColour`、`LaneBackgroundOddColour`、`ScratchLaneBackgroundColour`、`LaneDividerColour`、`ScratchLaneDividerColour`、`HitTargetBarColour`、`HitTargetLineColour`、`HitTargetGlowColour`、`ScratchHitTargetBarColour`、`ScratchHitTargetLineColour`、`ScratchHitTargetGlowColour`、`MajorBarLineColour`、`MinorBarLineColour`、`LaneCoverFillColour`、`LaneCoverShadeColour`、`LaneCoverFocusColour`、`PlayfieldBackdropColour`、`PlayfieldBaseplateColour`。accepted value 必须沿用既有 `tryParseColour`：恰好 3 或 4 个逗号分量，各分量经 invariant `byte.TryParse(NumberStyles.Integer)`；RGB 补 alpha 255，RGBA 原样保留 alpha（包括 0），现 parser 接受的正号、leading zero 与 `-0` 也不得在 provenance 层改写规则，错误分量数、空分量、负数、超 255、小数、hex 或非 ASCII 数字不声明。valid duplicate、重复 bucket 与同一 decoder 的 repeated `Parse` 继续 last accepted，后续 malformed value 不得擦除此前 declaration，pending-before-`Keymode` 保持现 decoder 归属。当前 `Enum.TryParse` 会让部分逗号 composite source key 折叠到已定义 lookup；这种 key 必须继续写入 public mutable `Colours` compatibility view 以保持生产行为，但绝不能进入 exact accepted sidecar，exact declaration 后的 composite overwrite 也不得改变 sidecar。factory 只读 sidecar，手工或 decode 后对 `Colours` 的 forge/overwrite/remove/clear/late-add 均不得伪造、擦除或改变 snapshot。catalog、snapshot 与 factory 保持 BMS-internal、source-specific、process-local；它们不是 neutral colour/slot/author manifest/wire ABI，不执行 contrast/theme default、renderer transformation、validation、fallback 或 production wiring，也不得借此在引擎中新增主题色或默认视觉。
10g. native `[Bms]` geometry accepted provenance 的 closed set 固定为 exact、区分大小写的 `PlayfieldWidth`、`PlayfieldHeight`、`NormalLaneWidth`、`ScratchLaneWidth`、`NormalLaneSpacing`、`ScratchLaneSpacing`、`HitTargetHeight`、`HitTargetBarHeight`、`HitTargetLineHeight`、`HitTargetGlowRadius`、`BarLineHeight`、`LongNoteBodyWidth` 十二项；`HitTargetVerticalOffset` 必须继续排除并锁在既有 timing/scroll 合同之外。accepted value 完全沿用既有 invariant `float.TryParse(NumberStyles.Float)`：正负号、小数、指数、负零、`NaN`、正负 `Infinity` 均可声明，overflow 可成为正负 Infinity，underflow 可成为带原符号的零；这些只记录 parser accepted source fact。空值、thousands/comma decimal、hex、`∞`、非 ASCII 数字或 trailing junk 等 parser-rejected malformed value 不声明，也不得擦除先前 accepted declaration；valid duplicate、重复 bucket、pending-before-`Keymode` 与 repeated `Parse` 继续既有 merge/last-accepted 语义。既有 `geometry_keys`、parser 和 public `Geometry` compatibility 行为不得改变：逗号 composite raw key 若经 `Enum.TryParse` 折叠到已定义 geometry lookup，仍只写 public view，绝不能进入 exact sidecar；exact declaration 后的 composite overwrite，以及手工或 decode 后对 public `Geometry` 的 forge/overwrite/remove/clear/late-add，均不得伪造、擦除或改变 provenance snapshot。catalog、snapshot 与 factory 必须保持 BMS-internal、source-specific、process-local；这不是 finite/positive/range/cross-field/screen-space validation、layout descriptor/solver、neutral config、author manifest或wire ABI，也不接 production lookup、fallback、renderer、`SkinManager` 或 reload。
11. lane-resource field catalog 是 closed process-local taxonomy，不是 manifest/serialization ABI；snapshot 必须绑定 exact immutable topology、防御性复制、确定性排序、拒绝 null/未知字段/重复 lane-field，缺项为 `Absent`、显式空资源名仍为 `Declared`，安全字符串不得展开资源名。public legacy mania factory 只因跨 ruleset 程序集桥接而公开，不得当作作者/plugin/package/script API。
12. lane-resource candidate adapter 只发出 canonical marker 前的 selected-package providers，并保持 plan 顺序；完整调用方顺序仍为 beatmap-local → selected candidates → ruleset resources → explicit canonical。lookup 必须匹配 exact topology、canonical field 与该 field 的 semantic descriptor；缺声明不得 materialize，ini candidate 不得制造 `Suppress`，取消异常不得转换为 fallback。lane-resource component owner 与 topology publication owner 是两套不同的 process-local revision 合同，不得混用 revision 语义；它们自身也不等于完整production lifecycle manager。

## osu 社区式制作者合同

1. `.osk` 是 V1 的正式分发单位；打开/拖入即可导入，解包后是根含 `skin.ini` 的普通可编辑目录。managed/external folder 是作者工作区与高级管理面，不能取代 `.osk` 的社区交换地位。
2. `[General]`、`[Colours]`、`[Mania]` 的语法、`Keys:` 分桶、既有素材名、`name-{n}` 动画序列、资源缩放/缺项 fallback 等共同语义以当前 osu legacy compatibility 为基线；BMS 不另造一套同义基础格式。
3. `[Bms]`、declarative manifest 和 optional script 是 OMS 对第一类 BMS ruleset 的版本化扩展。扩展文件必须可被 OMS validator 识别并产生清晰诊断，不要求作者编译 DLL，也不得冒充上游 osu! 已原生支持的格式。
4. 只做 mania、只做 BMS 或同时做两者的 `.osk` 都合法；`oms-simple.osk` 与 `oms-complex.osk` 必须在一个包内同时提供 mania/BMS，并可作为第三方作者的真实参考源。
5. common mania assets/ini 在 OMS 中的行为须有代表性社区皮肤 fixture；OMS 生成的组合包若宣称 mania-compatible，也须验证其 mania 部分不会因 BMS 扩展而改变。
6. 制作者套件（Skin Authoring Kit）至少包含：两内置包的可编辑源、带注释模板、字段/素材/事件/layout/capability/budget 参考、validator/diagnostic 用法、打包与导入说明。它不是 SDK DLL，也不是第三种 package 格式。

## fallback 与最小可玩约束

1. V1 lookup 必须区分 `Provide`、`Inherit`、`Suppress`。`null`/缺文件继续表示 `Inherit`，不得同时承担作者主动关闭语义。
1a. 三态应由平行的 `SkinSlotResult<T>`/gameplay provider（或等价显式类型）承载，不直接改变现有 nullable `ISkin` ABI。`Drawable.Empty()` 不能长期冒充 `Suppress`；`Provide` 构造/资源失败必须降为 `Inherit` + 诊断。
1b. `default(SkinSlotResult<T>)` 必须是 fail-open 的 `Inherit`，默认 requirement 必须是更安全的 `Critical`。resolver 严格按调用方给定顺序解析，不得自行重排；首个有效 `Provide` 或 optional `Suppress` 终止，critical `Suppress`、provider/构造/validator 失败记录结构化诊断后继续。
1c. 结构化诊断至少包含 slot、provider 与稳定原因码；持久化或写入仓库时不得携带用户绝对资源路径。resolver 不拥有候选值生命周期，也不得自动 dispose 被 validator 拒绝的 `Drawable`/`IDisposable`：provider 必须先完成基础验证，并与最终消费方显式冻结缓存、parenting 与回收责任。
1d. `SV1-1` 内部 semantic taxonomy 固定为 26 个 slot family。critical 仅为：`playfield.lane-surface`、`playfield.judgement-line`、`object.note`、`object.long-note.head`、`object.long-note.body`、`object.mine`、`playfield.lane-cover.fill`。optional 为：`object.long-note.tail`、`playfield.key`、`effect.key-flash`、`effect.hit-explosion`、`hud.judgement`、`hud.combo`、`hud.gauge`、`hud.text`、`playfield.bar-line`、`stage.background`、`stage.foreground`、`playfield.backdrop`、`playfield.baseplate`、`playfield.lane-cover.decoration`、`playfield.turntable`、`playfield.laser`、`bga.viewport`、`bga.frame`、`decoration`。
1e. taxonomy ID 使用小写 ASCII 点分段、ordinal/case-sensitive，不从 CLR enum/type/本地化文本派生；未知或畸形 ID 不得动态注册或默认为 optional。ID 只用于内部语义与稳定诊断，当前不是作者 manifest ABI；catalog 顺序也不表示 provider precedence、绘制顺序、z-order 或 layout。
1f. lane/keymode/side/result 等 context 必须由 `GameplaySkinSlotLookup<TContext>` 与 descriptor 分离承载。catalogued resolution 的 requirement 只能来自 descriptor；旧 raw overload 只保留给 uncatalogued compatibility，未来生产接线不得用它绕过 taxonomy。
1g. LN head/body 保证必要可读性，tail cap 可 suppress。lane cover 只在玩法启用时请求 critical `fill`，该视觉必须挂在引擎强制 geometry/clip host 内，皮肤不得改变真实遮挡范围；`decoration` 仍 optional。`bga.viewport` 只呈现引擎拥有的只读 content surface，不拥有 player/timeline/clock。
1h. catalogued diagnostic 的 `SlotId` 是可持久化稳定字段；进程内 `Slot`/`Exception` 不得序列化，安全 `ToString()` 也不得展开它们。`ProviderName` 必须是非敏感 authority 名且不得含绝对路径；当前代码只靠 provider 合同约束，不能宣称自动脱敏。
1i. source-aware lane-resource reference 可以把未验证 resource name 交给 materializer，但稳定诊断、JSON 与安全字符串不得展开它；同一 raw name 在不同 source/Keys/lane/field 下必须保留 authority 区分，不能只按字符串名共享或去重。
1j. lane-resource materializer 必须在返回前由 revision owner 持有 component 并完成基础验证；winner 与被额外 validator 拒绝/异常隔离的 component 都继续归 owner，resolver/provider/consumer 不得单独 dispose。active 与 provisional owner 不得跨 revision 混用：失败 provisional 只 dispose 自身；成功替换先原子切换并 detach superseded consumer，再 dispose 旧 owner；teardown 同样先 detach 后 dispose。
1k. revision-owner只是internal contract/fixture，不证明真实`.osk` containment/存在性/解码、纹理预算、Drawable parenting/thread affinity、缓存、整包原子reload或完整production lifecycle；这些责任不得下沉给shared resolver。
1l. 资源 declaration 与实际 texture/frame 必须在同一个精确 provider/package authority 内解析；禁止先从聚合配置取声明、再从聚合 texture store 取同名素材，造成跨皮肤拼接。
1m. 有效的 beatmap-local 直接视觉继续高于 selected package；只有高优先级来源缺失、损坏或验证失败时才 `Inherit`，三态不得默认穿透有效 beatmap-local provider。
1n. Realm登记的外部package materialization必须使用不可变的文件名→内容身份快照并绑定package revision；路径越界、大小写冲突、重复名或authority冲突均fail-open为对应slot的`Inherit`，不得从另一package补齐。
1o. 文件素材必须在解码前检查输入字节、图片尺寸/像素、帧数与累计预算，并在解码后再次核对实际资源；先无界解码、再检查预算不算防护。当前普通短键/长条 head/body/tail runtime 限值只是安全上限，不是最终 author ABI。
1p. 初次装载与 live replacement 的 package IO/解码都不得发生在 update thread；新视觉完整准备并属于当前 revision 前保持现有视觉或 critical fallback，取消、过期或失败结果不得发布且必须释放其临时资源。
1q. 当前普通短键/长条 head/body/tail 纵切只保证 per-component 安全发布与回落，不代表 `SV1-2` 的整包原子 reload；ini、scene、script 与所有素材共同成功后一次切换仍是后续 gate。
1r. runtime 的 raw/decoded/frame/texture cap 不等于 `.osk` importer 的总压缩/解压字节、解压比或 zip-bomb 防护；两道 gate 必须分别实现和验收。
1s. 当前选中外部BMS package的native生产窄路径只接受ordinary note、`LongNoteHead`、`LongNoteBody`与`LongNoteTail`：四者按element + keymode + canonical lane/scratch绑定slot，解析同一精确package revision；连续`name-0`、`name-1`…固定60 FPS。note/head/body是critical且不可suppress；声明失败必须落到可见rescue，不能由低层裸同名texture补齐selected坏声明。该路径不得改变LN/CN/HCN、尺寸、裁剪、layout或其它provider authority。
1t. `LongNoteTail` 保持 optional：未声明为 `Inherit`，有效静态/连续编号帧为 `Provide`；当前 producer 不产生作者 `Suppress`，空值、坏资源或 protected 链底透明都不得解释为 `Suppress`。selected 失败后下层自己的完整 tail 组件可以接管，只有同名文件而无 accepted declaration 不能补件；protected `OmsSkin` tail 必须 `Alpha=0` 且禁止 aggregate texture lookup。beatmap-local 直接 Drawable 仍优先，tail cap host、判定和 LN/CN/HCN 不变。
1u. `LongNoteBody` 只消费 decoder-time accepted `[Bms] NoteImage{lane}L` / `NoteImageSL` / `NoteImageS2L`，可为静态图或 60 FPS 连续编号帧。它必须经过唯一共享标量 geometry resolver：`LongNoteBodyWidth` 默认 `0.5775`，只接受 finite 且 `0 < width <= 1`；absent、non-finite、小于等于零和大于一分别保留稳定 typed rejection reason并逐字段回落默认。有效 body + 非法 width 仍发布同组件默认宽，只有 body 资源整体失败才 `Inherit`；selected 失败后低层裸同名文件或裸 width 均不得拼件，低层自己的完整 body 组件可接管。
1v. body texture/frames 与 accepted/default width 必须携带同一个 exact parsed `skin.ini` 内容身份和 package revision进入 prepared material并一起发布；renderer 禁止再从 aggregate skin 反查 width。selected-package/default body必须共用真实Idle/Holding/Broken状态宿主：active alpha `0.8`、broken alpha `0.32`、后续状态tint/fade `80ms`，异步首次挂载或material replacement立即投影hold当前态，HCN regrab只投影gameplay authority。不得为此修改`DrawableBmsHoldNote`状态authority、body拉伸/裁剪或LN/CN/HCN规则；该单字段policy不等于完整`SV1-3` layout/screen-space validation。
1w. 成功 preparation cache 不感知同一 `BmsLegacySkin` 实例内的原地 source revision 变化，active实例必须保持immutable，不得把磁盘变化混入旧material。C2显式manual Reload只能构造绑定exact content revision的新instance/package owner并经统一barrier替换；same-instance refresh、watcher或逐component cache清空不得冒充revision publication。
2. 三态链只替换 gameplay package 内的组件解析：用户所选 `.osk` → ruleset adapter/compatibility → 随发行物只读携带的 `oms-simple.osk`。现有 `BeatmapSkinProvidingContainer` 的谱面内皮肤 enable/colour/hitsound 语义，以及 `RulesetSkinProvidingContainer` 注入 ruleset resource skin 的相对 authority，在另有迁移决议前必须保持；不得把完整现有链误写成只有上述三层。
2a. `Suppress` 默认只作用于声明它的 gameplay package slot，不得越权穿透或屏蔽更高优先级的 beatmap-local provider；若未来需要改变该边界，必须单独冻结 precedence fixture 和用户迁移规则。
2b. 测试中名为 `oms-simple` 的 fake provider 只证明 canonical 末端语义，不代表真实 `oms-simple.osk` 已制作、校验或接入生产 fallback authority。
3. 以下 gameplay-critical 元素不得被完全 suppress：lane/scratch 可辨识、note、LN、mine、判定位置，以及启用 lane-cover 玩法时的 cover 几何/遮挡。用户包缺失、损坏或非法时必须逐组件回落 `oms-simple`。
4. key/keyflash、hit explosion、judgement display、combo、gauge visual、文本 HUD、turntable/laser、BGA frame、装饰和其它非核心视觉可以显式 suppress；其 gameplay 状态仍在引擎中正常运行。
5. `BmsSkinTransformer.providesBuiltInFallbacks = skin is OmsSkin` 只记录当前迁移基线：在 `oms-simple` 达到 mania/BMS parity 前不得贸然删除；达到 parity 后必须由文件包 fallback 取代并退出产品渲染链。
6. 最终产品不得存在主题化程序化 fallback、硬编码色块/辉光或私有默认视觉。引擎代码可以并且必须保留通用 scene renderer、note/LN host、对象池、layout/event bridge、资源隔离与 gameplay truth，但所有具体颜色、素材、节点和动画来自 `.osk`。
7. `oms-simple.osk` 是不可被用户修改/删除的 canonical fallback：发行构建锁定版本/hash，启动验证并可从只读 canonical copy 原子恢复工作副本。canonical copy 自身失败属于安装完整性故障，应阻止进入 gameplay 并给出修复指引，禁止静默生成程序化视觉。
8. `oms-complex.osk` 与 `oms-simple.osk` 同权使用公共 package/scene/script API；可以作为默认选择或展示包，但不得成为 `oms-simple` 之下的隐藏第二 fallback。

## scene、事件与脚本约束

1. 声明式 scene/state-machine 是首选作者面；脚本用于声明式能力无法合理表达的组合逻辑，不能成为显示普通 note/key/judgement 的必需条件。
2. 外部拥有视觉内容不等于外部直接拥有 framework Drawable/tree。引擎必须提供通用 scene renderer、global/lane template、pooled note/LN instancing/scroll/clip host、effect pool、z-order、资源和事件调度；删除的是主题/效果硬编码，不是内部通用表现运行时。
2a. 声明式层必须有 typed property binding/variant/template，可直接表达 gauge value→clip/scale、combo value→text、result key→sprite variant 和 per-lane context；动画编译成 gameplay-clock transform，不能强迫普通 gauge/combo/judgement 使用脚本 `on_update`。
3. manifest 使用稳定 node type ID + allowlist，不得复用序列化 CLR `Type` 并反射实例化的 Skin Layout Editor JSON 作为第三方 ABI。
3a. V1 只开放 allowlisted blend/effect/shader preset；任意 shader/驱动特性不属于 V1 完成条件，后续扩展也必须 capability/version/budget 化。
4. V1 event family 至少覆盖 lifecycle、layout、input、object/LN/mine、judgement/offset、combo/score/gauge、beat/measure/BPM/STOP/scroll、BGA/POOR。
5. event envelope 至少携带 `apiVersion/epoch/sequence/gameplayTime/layoutRevision`；attach/reload 先发完整 snapshot，seek/retry 发 reset/new epoch，之后才发 edge。result 使用 neutral key + 可选 ruleset-native ID，不泄露内部 Drawable/HitObject。
5a. `GameplaySkinEventEnvelope` 是 process-local engine contract，不是 serialisation、script 或 author manifest ABI。外部 package 只能消费已验证 DTO，不能构造 envelope 或发布 gameplay truth；ruleset adapter 只能提交 neutral primitives/调用 shared concrete payload factory，shared dispatcher 独占 `apiVersion/epoch/sequence/gameplayTime/layoutRevision` 盖章。即使 friend assembly 技术上可见 internal member，也禁止直接派生 payload 或发布 envelope。
5b. `gameplayTime` 使用 gameplay clock 的毫秒域，必须 finite；lead-in/storyboard 可为负，同一时刻可有多个事件并由 sequence 定序，绝不是 wall/update clock。当前 envelope 可承载正数 future version 以便 fail-closed 拒绝，但 canonical cursor 只接受显式支持的 V1，版本不得在 attachment 内变化。
5c. payload hierarchy 由 shared engine 拥有；每个 concrete payload 必须 sealed、不可变、防御性复制集合且 ruleset-neutral，不得暴露 `Drawable`、`HitObject`、`JudgementResult`、`HitEvent`、`Bindable`、clock、Realm object 或其它可变 native state。`JudgementResult`/revert 等事实必须在 authority 回调栈内复制 primitives，不能排队保存对象引用后再读。
5d. internal cursor 只校验 capability/family filtering 前的完整 canonical stream，绝不排序、补洞、重放或自动修复。首次 attach/reload 的完整 Snapshot 可从任意非负 mid-session epoch/sequence high-water 建立状态；之后 epoch 严格 `+1`、同 epoch sequence 严格 `+1` 且 time 非递减。Reset 必须位于下一 epoch 的 sequence 0，可重锚前后跳的 time；layout revision 在 attachment 全程不回退，Snapshot/Reset 可保持或推进，Edge 必须等于当前 revision。任何拒绝都不得推进 cursor，计数器耗尽时 fail-closed，禁止 wrap。
5e. Snapshot 和 Reset 都必须原子携带完整 baseline；Reset 不是“先清空、稍后再等 Snapshot”。当前只有fake payload的envelope/category/order fixture，不能证明完整baseline、真实attach/reload/seek/retry producer或delivery；在concrete payload family与lifecycle bridge落地前不得把本合同写成可用event runtime。
6. 连续 scratch/scroll 采用固定采样/节流/合并合同；脚本不得要求每个原始轴采样或每帧谱面对象位置回调。
7. 事件必须按 gameplay clock 排序且 payload 不可变；脚本不得反查 `DrawableBmsRuleset`、遍历父节点或订阅内部 bindable。
8. replay、seek、retry、pause 和 hot reload 必须定义状态重建；随机数只来自引擎提供的确定性 seed。
9. 脚本 VM 必须支持可抢占 instruction quota 与 heap quota；只在回调返回后检查 stopwatch 无法防无限循环，不可作为安全预算。
10. package/runtime 限制总压缩/解压字节、单资源解码像素、总 decoded bytes/纹理/atlas/动画帧、global/lane/note/effect 节点和每帧预算；超限/异常只熔断脚本/scene/对应组件，不能中断 gameplay。
11. 脚本 capability 必须声明；允许的可选能力须 per-skin 显式授权、可查询、可撤销。网络/任意文件/反射/进程/线程/原生库/gameplay writes 永不授权。
11a. capability grant 必须同时满足：package 显式 request、ID 存在于 engine closed allowlist、required host feature 当前可用、需要 per-skin authorization 的项已由当前 skin 获准，并且未命中 hard deny。任一单独的 request/support/grant snapshot 都不能制造权限；unknown 不动态注册，hard deny 永远优先。
11b. capability ID 是非敏感小写 ASCII opaque token，不得嵌入包名、用户数据、资源名或路径。当前 CLR carrier/diagnostic/JSON 仅为 process-local decision 与隐私 fixture，不是 manifest、持久化或 script ABI；未来 parser 必须先实施 ID 长度、request 数量与 package budget。
11c. hard-deny catalog/classifier 是 closed allowlist 后的第二道 fail-closed 屏障，不是对任意同义词的穷举。明确禁止 gameplay input/lane/action/layout/judgement-line/cover/scroll/timing/clock/judgement/score/combo/gauge/chart/beatmap/BGA mutation、Realm/config authority，以及 network/arbitrary filesystem/reflection/process/thread/native family。禁止 `gameplay.layout.write` 不等于禁止经 schema 校验的声明式 geometry；package-scoped resource read 也不等于 arbitrary filesystem。
11d. 当前内部 classifier 对明确 deny root 及 descendant、以及 terminal mutation action 生效；只读 event fixture 以 terminal `.read` 区分，因此 `reset/seek/create/update` 等事件名可出现在前序 segment。该命名规则尚不是 author ABI，未来 catalog/manifest 定稿不得把只读 event family 误分类成写 authority。
11e. negotiation result 只能携带 immutable granted ID 与结构化 denial，不得携带 delegate/service/object/authority handle；同一 ID 不得同时 grant/deny，hard-denied ID 不得进入 grant。撤销通过重新协商产生新 snapshot，但每个 future host API 仍须实际 runtime gate；本合同不证明旧 scene/script 已原子停用。
11f. 当前没有真实production capability、package identity、授权存储/UI、required/optional、explicit deny状态、layer activation/fallback、protocol version或sandbox runtime。`NoAdditionalAuthorization`只可用于经产品策略确认的低风险baseline；允许的可选package能力仍必须per-skin authorization。
12. 脚本编译和文件 IO 不在 update thread 执行；不得在事件回调做阻塞操作。
13. 任何脚本引擎选型先通过 preemption/capability isolation、license、Windows 打包、性能/GC 和调试诊断 spike。采用 Lua 不等于兼容 `.luaskin` 或 beatoraja runtime。
14. 受信任 C# `ISkin` provider 可留作开发/高级扩展，但用户可分发 V1 皮肤不得依赖编译 DLL。

## playfield 与 BGA 布局约束

1. shared runtime 只定义 neutral `GameplaySkinLayoutContext`；BMS adapter 必须产出唯一不可变 `BmsGameplayLayoutSnapshot`：screen/safe bounds、命名 z-layer/clip/input-pass-through、playfield/stage groups、lanes、judgement line/cover、BGA viewport、HUD anchor slots、keymode 来源、style、明确 `PlayerSide`、scroll direction，以及每轨 logical/visual index、source channel、action、role、最终 rect。
2. playfield 顶边默认贴屏幕顶边，当前 `HitTargetVerticalOffset=0` 下保持 `scrollLengthRatio == 1`。皮肤改变视觉尺寸不得隐式改变 GN/判定时序。
3. 5K/7K 必须覆盖：P1 左停靠+左皿、P2 右停靠+右皿、CenterP1 居中左皿、CenterP2 居中右皿。style 只改 visual order/anchor，不改 action binding。默认 BGA side 与 player side 对置：P1/CenterP1→右，P2/CenterP2→左；当前 CenterRightScratch 仍右上是待迁移缺口。
4. 9K BMS 与 9K PMS 均 center，但 descriptor 必须保留不同 keymode context；P1/P2 请求规范化为 center。
5. 14K 必须建模为两个 lane group：S1+K1–K7 与 K8–K14+S2，双皿位于两 deck 外缘，centre gap 明确且不改变 action identity。
6. BGA decode、timeline、seek、POOR 和唯一 content authority 归引擎。皮肤只取得只读 content surface/viewport/event，可加 frame/mask/opacity/装饰；不得自行创建独立 `BmsBgaPlayer` 或不同 clock。
7. 若 descriptor 暴露多个 BGA mirror viewport，它们必须引用同一 content authority。当前 14K 四角四 player 只是临时实现，不得冻结为 V1 合同。
8. BGA viewport、playfield 和 gameplay-critical HUD safe slot 在支持的宽高比/DPI 下不得不受控重叠；需要重叠的艺术表现只能在皮肤自有装饰层，不得遮断核心可玩层。
9. layout 变化须覆盖 5K/7K 四 style、9K BMS/PMS、14K 的自动 screen-space 测试和实机截图；不能只用 7K fixture 代表全部模式。
10. playfield、gauge、combo fallback、BGA 与 scene/script 必须消费同一 resolved snapshot；不得在各组件内按 keymode 重新建立默认 profile。skin geometry 变化以新 snapshot 原子发布。
11. geometry 值必须逐字段验证 finite、正值、合理范围、屏内与不重叠；非法/NaN/负值/零宽/超屏值逐字段回落并诊断，不得进入 `TotalRelativeWidth` 除法。
12. keymode 是 layout 前置 authority：snapshot 必须包含 detector source/diagnostic。sparse chart 不确定时使用明确纠正/override 流程，skin/layout 不得根据 lane 内容二次猜测。
13. topology 自动 gate 必须覆盖每条 visible/LN/invisible/mine/armed-keysound lane；所有边界用 lane count（含 scratch），不能用 key count 丢最右键或 14K Scratch2。

## G1 存储与恢复约束

1. 2026-06-30 00:05 之后的 G1/F2/Lua/mania adapter/reference-default 不构成当前能力，禁止整批 cherry-pick/apply；只可定点复用设计和测试教训。
2. schema 56生产Realm的只读清点结论已经闭合并持续生效：不得降schema、重新解释或自动清理当时保全的folder-backed `SkinInfo`、迁移归档、无authority orphan blob及`chartskin/`；任何后续处置仍须定点取证并单独批准。
3. managed relative path 与 external root 是不同 authority。V1 首批 preflight 只接受 drive-letter-qualified fully-qualified Windows path；语法本身不证明物理本地盘。managed/external native capture均将映射收窄为 exact physical `\Device\HarddiskVolume<uint>`，mapped network drive、SUBST、shadow/device alias均 fail-closed。UNC、device namespace 与盘符/volume root fail-closed。external只能通过manager-owned no-follow capture作为永久只读source，不能把绝对路径交给UI/contained storage，也不能让external root与`chartskin` exact/ancestor/descendant namespace重叠。
3a. 当前 `SkinFilesystemStorageResolver` 闭合schema 56引入、schema 57继续保留的存储声明与lexical/reparse preflight：Realm `.osk`不触碰Storage，folder必须`Files.Count == 0`，受管目录只接受`chartskin/<direct-child>`，folder protected/fixed-ID/DeletePending均拒绝；数据根、managed root、package root或external ancestor任一reparse均拒绝。managed与service-owned external production selection都会重新解析authoritative记录并取得opaque capture request；external还必须进入bounded exact registry set并持有physical proof到final线性化。返回的normalised absolute/relative path是敏感process-local值，安全字符串不得展开；`IsValid`不表示`InstantiationInfo`、包内容、选择资格、物理identity或后续mutation已授权。
3b. lexical preflight不是安全打开或mutation capability，pure capsule也不是其替代品：`File.GetAttributes()`存在TOCTOU，capsule只冻结producer实际交付的bytes，无法单独证明这些bytes来自preflight root、同一物理文件或一次稳定捕获。production受管目录factory必须只消费resolver-issued request经Windows native capture完整成功返回的capsule；scanner的held-root gate只闭合发现/reconcile，rename、staged import与managed delete各有专用held-root/no-follow write和按kind恢复gate，任何操作都不得直接拿normalised path执行I/O。
3c. capsule content revision 只表示规范资源名、长度和实际读取内容的确定性身份，不是 authority、physical/capture identity、semantic package validation、`InstantiationInfo` 或选择资格。精确 declared length 也不能单独证明同长度内容未在读取中变化；production folder 路径必须由 native capture 固定 held identity、metadata与inventory后再进入 capsule。
3d. managed capture request 只能由 `SkinFilesystemStorageResolver` 为合法 `chartskin/<direct-child>` 发出，构造受 internal issuer 约束，但它不是 security/filesystem/mutation capability；Realm/external/invalid declaration均不产生 request。Windows capture必须从 strict physical NT volume handle 逐段 handle-relative no-follow open，所有 directory/file handle持有到 capsule完成和 final pinned metadata/inventory/authority-link/package-root复验；reparse、未由resolver展开成长名的8.3/alternate alias、hardlink/重复 identity、unsupported mapping、busy writer与观察到的竞态一律 typed reject。成功前须先释放全部 handle，不得让 live stream/handle逃逸。该保证窄于 filesystem transaction；production selection现可消费其成功capsule，但request/capture本身仍不是选择授权或external capture。
3e. capsule 必须独占并清理 backing；resource view 非 owning，`Get`/`GetStream` 返回 defensive copy，view dispose 不得释放 capsule。失败/取消不得发布半成品且必须清理 provisional backing；active capsule 只能在所有 consumer detach 后退役。
3f. production managed/external folder factory必须走exact-capsule构造路径，不得让`Skin`默认添加的live `RealmBackedResourceStore`排在capsule前面。ordinary Realm `.osk` current reload必须fresh取得detached metadata和完整file declaration，逐blob核对hash并构造规范capsule/new owner；managed/external则使用各自held capture session。三者都不得用live refresh改变active instance；legacy Skin Editor已统一fail-closed，不再是此合同的例外。ordinary revision一旦发布，后续Realm record的file-declaration path、external或DeletePending projection漂移不得改变已发布selection/owner/revision，late consumer仍只取得active immutable owner；fresh reload/mutation admission另行重读，file-declaration path改变形成declaration mismatch时必须拒绝。不得把这类Realm record漂移误称为registry file drift。
3g. production folder factory的closed allowlist当前精确为`osu.Game.Rulesets.Bms.Skinning.BmsLegacySkin, osu.Game.Rulesets.Bms`，按ordinal完整匹配，并要求根`skin.ini`与公开exact-capsule四参数构造入口；不得接受派生类型、宽松assembly/type解析、历史`TrianglesSkin` fallback或live filesystem store。exact store必须携带明确marker并独占capsule，factory在所有成功/失败/异常路径只能转移一次owner且清理幂等；folder实例使用detached `SkinInfo` snapshot，不得保留live Realm object作为revision authority。
3h. folder selection只允许authoritative Realm记录满足对应managed exact-owner或external exact service-owner声明，并完整通过resolver/native capture/factory合同。请求必须在update thread进入且在线程门通过前不得递增generation或取消既有pending；普通Realm `.osk`保留既有线程兼容。所有请求的generation bump、authoritative Realm重读与final pair commit必须串在共享coordinator边界内；capture/factory前后双重复核record/path/owner/freeze，final commit只能使用本Realm重新取得的`Live<SkinInfo>`。configured managed/external preparation保留typed startup completion与generic mutation epoch fresh retry；external只进入显式dropdown，不得进入random/next/previous或hotkey隐式候选。same-value selection仍为no-op；同ID新内容只允许Settings的`Reload current skin`进入C2协议。任何invalid/unregistered/owner drift、capture/factory异常、stale generation、mutation race、reentrant或scheduler fault均保留旧pair并回收provisional owner；shutdown先封门/推进generation/cancel，再join所有capture/waiter/completion worker，晚到callback只能no-op且诊断不得泄露路径。
3i. committed skin selection bindable必须封闭binding graph：只能与同类guarded bindable建立受控同步，不得向普通`Bindable`、generic Dropdown或lease暴露可双向写入的提交面。UI只能使用本地展示bindable调用显式request surface，并从committed副本单向镜像；disabled/same-value请求不得启动capture，reentrant请求不得绕过precommit gate。
3j. folder的editor/export/旧rename/delete/undelete/file add-delete-replace/update-import/external-edit等通用Realm/package入口继续冻结。任何通用mutation即使经base/interface dispatch也必须在真正Realm事务内按ID重新获取authoritative记录后再判定；调用方传入的detached、stale或同ID伪造`SkinInfo`字段永远不能授权，旧importer也必须在清空文件前于同一事务复核。`SkinAuthoringAvailability`统一关闭legacy Skin Editor；`ExternalEditOverlay`、manager/importer的external-edit和update-import在mount/store/current pair变化前稳定拒绝，base/interface dispatch也不得绕过。directory-only rename与fixed-source staged import只能由本线专用operation消费既有authority/coordinator/journal/recovery；managed delete只能由fresh record-ID `CanDelete(Guid)`与manager-owned `DeleteSkinAsync(Guid)`消费，旧`CanModify/Delete`不得解冻。现有current button与Folder Skin Workspace managed行可作为两个Delete UI入口，但必须复用同一确认语义和专用surface；共享确认面只接record ID与immutable display label，不接`Skin`、`Live<SkinInfo>`、path或authority proof。Workspace其它行也只持committed ID/label/kind/capability hint并由manager fresh重读；Workspace不提供Reload，三源current reload只能从Settings唯一入口触发。
3k. Workspace records与journal support inspection属于manager-owned只读worker：它们可并发且不得占用mutation slot，但必须在同一shutdown封门下登记、取消、观察并同步join，确保Realm/native authority释放前无晚到读取；UI关闭只取消自身刷新lifetime，不得取消已开始的durable mutation。managed Open的row capability只是hint，执行时必须在初始与final Realm view都fresh证明record字段、owner、freeze及normalized path唯一；重复同路径声明一律不得把path交给host。
3k1. active revision对capture后的磁盘变化保持immutable；C2 manual Reload使用new owner + exact content revision，不修改旧instance。`CurrentSkinInfo`、`CurrentSkin`和`CurrentRevision`必须在同一update-thread barrier内提交；active capsule/owner等待所有participant与work lease detach后exactly-once retire，selection pair或`SourceChanged`都不得替代该证明。
3l. configured managed skin的异步capture与startup scanner必须按typed holder确定性协调：startup recovery→scanner持有`StartupSequence`外层lease；每次managed preparation记录startup sequence epoch与generic `MutationReservation` epoch。只有exact active startup/staged-import holder的typed completion，或authoritative mismatch已证明跨越completed startup sequence时，才可在后台等待并经update scheduler进入fresh retry；update thread不得等待。observation必须贯穿waiter、deferred callback与后续contention chain，在direct/chained排队前及持有retry short lease后复核generic mutation epoch；任一rename、managed delete或普通generic mutation reservation跨越都立即fail-closed，staged import保持独立typed completion且不借generic epoch。fresh retry必须重新验证shutdown/disabled、generation、旧current info/skin pair、authoritative Realm ID/record、path、exact owner、freeze、allowlist，并重新capture/factory；禁止复用旧capsule、factory或prepared authority。manual managed请求在startup占用期间仍即时`ManagedFolderOperationInProgress`且不得取消已开始的configured preparation；普通Realm `.osk`不进入该链。任何update-thread selection在解析或触碰filesystem声明前还须先做non-blocking coordinator admission，generic contention不得先越过resolver再拒绝。
3m. C2唯一产品触发固定为Settings → Skin的`Reload current skin`；Folder Skin Workspace无第二Reload，same-value selection仍no-op，scanner仍非watcher。ordinary Realm `.osk`、managed与external三源都以same record ID准备fresh immutable `SkinCurrentRevision`。live gameplay/gameplay preview由`RulesetSkinProvidingContainer`与`PlayerLoader`登记`LiveGameplayHost`，在任何source capture/parse/provisional prepare前确定性拒绝并提示退出后重试；其它attached且无staged receipt的视觉consumer同样拒绝，旧pair/revision不得先改变。
3m1. coherent视觉participant inventory至少包括：core/mania/BMS的ruleset/beatmap provider tree、BMS playfield/lane/geometry/Note/LN/pre-start/BGA及mania/core drawable；generic `SkinReloadableDrawable` family、ordinary `SkinProvidingContainer`、`StarFountain`、`PoolableSkinnableSample`、skin storyboard，以及Loader/Intro/Welcome/PlayerLoader pending graph。live provider tree本轮采用prepare前拒绝；其余只有提供可回滚staged swap者才可进入barrier，否则fail-closed。`BmsAsyncNoteDrawable`/`SkinnableContainer`可能挂在尚未alive的outer graph，首次inner admission与`SourceChanged`后rebuild都必须由GameHost update scheduler推进；outer仍为`Loading`时先等待`LoadState >= Ready`再启动inner，避免global callback对Loading graph执行update-thread `AddInternal()`。只有这两个non-alive host覆盖`SkinChangeScheduler`；其余consumer保留local scheduler的suspend/defer语义。base `SourceChanged`处理必须先在event调用栈内同步执行旧work invalidation，再用generation标记并调度唯一fresh rebuild；不得用第二个event handler与host scheduler竞速。BMS/Skinnable override必须在各自work admission gate内推进generation并exact claim pending owner/CTS；prepare install与finish publish都比较captured generation。base Dispose或publication shutdown必须先进入不可逆participant terminal，派生shutdown/dispose同样在gate内推进generation并claim work，晚到callback不得再admit、rebuild、adopt revision或发布。menu/shell/background/transition只纳入生命周期，不扩张为作者皮肤面。
3m2. lease-only holder至少包括`SkinBackground`/`BackgroundScreenDefault`跨fade图、playing sample tail、BMS async note/materializer work、manager/provisional/rollback operation、participant registry/retire queue与`PendingAsyncDrawableOwnership<T>`持有的未parent graph。它们必须持exact revision/work lease到真实callback/worker/fade/sample退出，不能因视觉已切B就提前退役A。
3m3. 已证明不直接持current owner者包括只聚合descendant的`SkinnableSound`/`PausableSkinnableSound`、guarded bindable/UI row/notification、由登记descendant承担资源的无skin字段wrapper、beatmap-local/ruleset built-in source及已禁用legacy authoring路径；证明只排除其自身，不得漏掉child participant。
3m4. reload prepare前后及commit时都必须复核participant generation、target generation、current selection/owner/revision和exact source content revision。所有可失败Realm/blob/held filesystem I/O、capture、解析、纹理/sample/materializer准备止于background prepare；commit只做已准备且可逆的内存引用交换。全部participant ready前B不可见；commit fault逆序rollback已交换引用并保A。prepare中attach或commit前detach使snapshot失效并有界fresh retry；late attach只取得已提交revision/lease。commit前取消保A，commit开始后的取消不得制造split。异步graph的framework callback与ownership sentinel必须使用同一scheduler保持FIFO；当前Editor mode graph固定到`ScreenContainer.Scheduler`，不能一半local、一半global。旧revision只有manager、participant、work与operation lease全部满足detach fence后才可在update thread exactly-once retire；shutdown须先claim participant immutable set并触发其terminal/shutdown hook，再由真实owner cancel/reap pending callback并join真实worker，最后同步detach/reap，manager不得替consumer释放work lease。
3m5. latest-wins允许新request在旧uncooperative worker退出前完成prepare/commit，但旧worker永不commit且其operation admission直到真实退出才释放；reentrant observer、throwing observer、scheduler fault、retry与cancel都不得拆分guarded pair。成功publication清理诊断必须以该请求generation精确守卫：同代startup contention最终成功可清为`None`；若`SourceChanged` observer重入并推进generation产生新的invalid/reentrant拒绝，outer completion不得覆盖其脱敏reason。所有诊断只输出稳定reason，禁止path、record/operation ID、physical identity或native异常正文。最终ini/manifest/scene/script/素材整包门仍到C6；C3～C6新增consumer必须同切登记participant/lease。
3n. external registration/capture的C1产品合同已闭合，仍须保持永久只读、strict local physical Windows no-follow capture、immutable capsule/manifest、service-owner只管Realm记录和fresh physical proof才授权source/collision。选择、重选或reload外部目录时，managed authority、full external registry snapshot与target package session必须持有到各自final validation；final callback必须重新取得对应selection/revision lease并复核generation、generic mutation epoch、current pair/revision、managed root、target proof与full registry declaration/physical snapshot。selection/reselection可在自身pre-publication线性化内compare exact declarations并按既有C1合同更新本服务的Name/Creator/Hash observation；same-ID current Reload只发布prepared in-memory owner/revision，既有service-owned Realm declaration与registration observation必须保持exact且不得在commit中写Realm。这个持有不等于跨越慢IO占用coordinator。latest distinct request必须能在旧capture阻塞时提交，旧请求不发布；shutdown取消/join必须释放所有root/package handle。noncurrent unregister仍直接pure-Realm compare-remove；current unregister先以统一barrier发布protected fallback并等待旧revision `ConsumersDetached`，再fresh复核fallback/current revision、full registry和exact service-owner record后pure-Realm remove。fallback/participant/fresh compare/Realm任一步失败必须借old-revision operation lease恢复exact旧pair/revision并保留record；所有结果source零I/O、零变化。
3o. external的Realm publication/removal与全部managed mutation必须加入shared coordinator或等价exact集合epoch，不能只做瞬时path检查：register/unregister commit、selection publication、reload final validation、scanner reconcile、rename/staged-import/delete admission及各自final collision point必须能证明其观察的service-owned external记录集合、ID/path/owner与held physical proof仍为同一版本；并发新增重叠external、unresolved/null/foreign owner、identity drift或集合漂移一律fail-closed。external注册数、path depth、单operation枚举量及同时held proof/handle数必须有硬预算，不能让大量合法记录形成handle/延迟DoS。configured external selection不能因“不归scanner维护”绕过`3l`：它必须与startup recovery→scanner取得确定性顺序或只等待exact typed startup completion后经update scheduler fresh retry，保留generic mutation epoch跨越即拒绝、direct/deferred/chained latest-wins、reentrant、update-thread non-blocking及shutdown exact claim/reap/join；fresh retry重做Realm/path/service-owner/freeze/physical capture/factory，不复用旧capsule。external持久metadata/hash只是registration/selection的last observation而非byte authority：注册后原位修改不污染active，same-ID selection继续no-op；安全screen显式Reload才允许以fresh capsule取得in-memory新revision，成功不得改写registration observation。external进入settings dropdown，但继续从random/next/previous及对应hotkey候选中排除。noncurrent unregister只在真正Realm事务内按record ID重读并compare-remove exact versioned service-owner记录，不解析或触碰source；source缺失、不可读或漂移不得阻止移除陈旧注册。current unregister必须遵守`3n`的fallback/detach/old-revision lease与fresh compare-remove事务，不能复用noncurrent的“先证明两半非目标”捷径，也不能在失败时dispose prior owner。
4. 每种受管目录真实mutation必须先冻结产品语义。rename只改变工作目录名和同一Realm记录的managed path，不改变`skin.ini`/展示元数据或包内容；staged import把OMS为本operation持有的固定provisional副本同卷identity-preserving no-replace move到空managed direct-child，并从最终exact capsule一次发布新record，不修改包字节或自动选择；managed delete只允许settings current确认框或C1 managed行级确认框经同一独立record-ID async surface删除eligible `chartskin/<direct-child>`，必须先收敛current fallback，再物理删除并hard-remove exact authoritative Realm record。三者不得由旧通用入口或底层实现猜测语义。
4a. 对已有记录的rename/delete，每次操作必须在真正Realm事务内按ID重读，并复核唯一、folder-backed、`Files`为空、非external/protected/fixed-ID、合法direct-child、exact scanner owner及该操作允许的`DeletePending`状态；这些只提供资格，永远不是filesystem capability。staged import不存在可预信任的既有owner，必须由journal operation identity与服务自身的source/target验证授权，并在冻结的线性化点通过scanner同等级注册/capsule/Realm冲突门后一次发布新记录；直接写owner token或让普通scanner消费plan均不构成合法注册。既有source只能从held `chartskin` root固定direct-child no-follow physical identity；staged-import target child name只能由可信internal caller显式提供，并表示为同一held root下经NFC/Windows/direct-child规范、case-insensitive physical/Realm collision与absence验证的name slot，不能伪造physical identity、覆盖、merge或自动suffix。rename与staged import移动后target identity都必须与held source identity exact一致，并在final-verify后才允许各自Realm publication。staged source只能来自固定staging authority并先固定完整identity/capsule，不能把调用方传入的任意live path当能力。resolved-root containment、exact/ancestor/descendant碰撞、大小写/NFC、reparse/symlink、hardlink/重复identity、busy writer与最终复验必须fail-closed，禁止字符串前缀授权。
4b. filesystem与Realm不能组成同一原子事务；首个外部可见步骤前必须持久化版本化、可校验、脱敏的operation journal，明确prepared/filesystem-applied/Realm-applied/committed等可恢复阶段。取消、异常、进程终止或部分失败后，启动必须先做幂等journal恢复再运行scanner；无法安全判定forward/rollback时保持journal并冻结相关路径，不得让scanner把半成品解释成普通新增/缺失或执行negative cleanup。
4c. scanner、selection与mutation必须共享明确的协调authority与线性化点；scanner实例自身的串行门不足以授权mutation。coordinator必须原子报告当前exact holder，不能用外部`IsRunning`或全局completion epoch猜测争用来源；只有startup sequence与staged import可签发selection retry completion，generic short scope/rename/delete reservation不得签发。合同须覆盖scan snapshot与Realm commit之间的竞态、in-flight selection的owner/path复核、mutation取消及shutdown join，保证同一physical identity不会被并发rename/import/delete分裂成多个逻辑身份。
4d. foundation、directory-only rename、staged import、managed delete与full ManagedCopy已由Folder Skin Workspace/manager surface提供真实caller并通过C1退出门。managed row Delete只捕获committed ID与immutable label，调用fresh `CanDelete(Guid)`与`DeleteSkinAsync(Guid)`，不得为noncurrent row构造或选择`Skin`；current目标必须先由C2 held capture证明exact source/content revision等于current revision，经统一barrier发布protected fallback并等待旧revision detach，之后才可创建C1 journal或开始physical mutation。current/noncurrent、dialog后切换、同名不同ID、record/owner/path/hash/DeletePending/external generation/revision漂移、split、blocked/late participant、double-click/reentrant、取消与shutdown必须继续证明没有第二authority或unobserved fault。full ManagedCopy只允许fresh service-owned external held capture、paired capsule/manifest和OMS fixed staging，禁止任意path/普通递归copy/overwrite/merge/suffix；register/select/restart/unregister不隐式复制，任何流程都不得写改删external源。旧通用mutation与thin stager继续冻结。
4e. 当前公共foundation的generic mutation reservation与staged-import reservation必须独占且不可同线程重入；startup sequence只允许同线程嵌套短临界区，以支持一个启动外层lease连续执行recovery→scanner，普通短临界区本身也只允许同线程嵌套。阻塞participant的lease wait必须可取消且不能存在ownership发布空窗；update-thread selection只用non-blocking admission。scanner从native discovery起持有共享边界直到Realm reconcile返回，selection最终authoritative重读/冻结检查/pair发布也在同一边界；mutation session可跨线程交还，但未终结durable intent的dispose必须保留journal并粘性冻结关联路径。
4f. canonical journal固定使用稳定文件名与strict version dispatch，严格UTF-8/固定schema/类型/重复字段/checksum/**1 MiB**大小预算；新intent只能从Prepared开始，显式phase图单调推进，terminal不可重写且不同operation intent不可覆盖。reader必须对`(version, kind, phase)`使用封闭白名单；ManagedCopy的Copying/ProvisionalReady对legacy v1/v2/v3非ManagedCopy kind均是Invalid，不得因enum值可解析就接受。current writer只写v3：Rename/StagedImport/Delete保留旧数值与phase语义，ManagedCopy新增kind及Copying/ProvisionalReady，并在同一payload durable绑定external registry generation/full digest/non-overlap disposition；每个checkpoint均write后exact reload并重新inspection，publisher/action不得早于phase允许的proof。pre-C1 v2与legacy-v1按frozen schema dispatch，绝不补写v3字段、重解释或创建新旧version intent。terminal journal删除前必须在exact Realm/held authority下复验并compare-delete，删除后只有fresh inspection确认Missing才可解冻；不得把无法证明的Missing变成Missing+freeze旁路。
4g. staged source authority固定为data root下`skin-mutation-staging/{operationId:N}`，不接受调用方path/token。manager-owned ManagedCopy从fresh external held session取得paired capsule/manifest，只在single v3 intent durable并exact reload后创建operation-owned provisional；文件bytes不重开external path，目录按manifest用destination handle-relative no-follow/no-replace primitive重建。staging root、source与既存managed root必须同volume并持有identity；import不得创建或替换managed root。Prepared publication plan的record ID等于operation ID并绑定target/root/version，但不是Realm写权限，ordinary scanner不得消费。首字节前取消可exact rollback，首字节后由recovery收口；foreign/identity drift或非空不完整副本保持freeze。
4h. current button或managed row打开确认框时的pair/affordance都不是authority。确认后operation先取得并全程持有managed mutation reservation与held exact target/source/content authority，但此时不得创建journal或触碰physical tree；再在C2 publication transaction中fresh重取`CurrentSkinInfo`/`CurrentSkin`/`CurrentRevision`与participant generation。coherent noncurrent不建立fallback且之后由C1 durable `NotRequired`表达，current则必须在update thread一次发布与`OmsSkin.CreateInfo()`逐字段一致的protected fallback revision，并等待旧revision `ConsumersDetached`。fallback/participant/source/split失败或该边界前取消都不得写journal、移动/删除目录或改变source，且须保留或恢复exact A并释放reservation/session。只有fallback+detach成功后才写C1 durable Prepared/`ProtectedPairCommitted` receipt；首个physical步骤前仍fresh复核fallback current revision、target与held authority。此后沿C1 journal/tombstone/Realm/recovery合同收口；首个physical步骤后的uncertain outcome只保证保持protected fallback与durable intent，不承诺恢复A。`NotRequired`仅当三元current authority coherent且都非目标；任一split、fallback invalid、revision/receipt漂移都拒绝或冻结。C7前protected fallback authority固定为程序化`OmsSkin`。
4i. directory-only rename中，目录名是工作区存储身份，`skin.ini`及Realm展示字段是作者内容。rename只改变一个`chartskin/<direct-child>`目录项及同一authoritative record的`FilesystemStoragePath`；不得发布新record、伪造scanner owner、修改`Name`/`Creator`/hash/revision或写包内容。
4j. 真实NTFS取证确认：即使所有descendant verification handle均允许`FILE_SHARE_DELETE`，非空目录在descendant handle仍打开时执行`NtSetInformationFile(FileRenameInformationEx)`仍返回`STATUS_ACCESS_DENIED (0xC0000022)`，flags `0`与诊断用POSIX flag `0x2`无差异。因此rename必须在最后一次完整held-tree preflight与caller取消检查后释放仅descendant handles，继续持有exact managed-root和source-directory physical identity，不再观察caller cancellation，并立即执行held-root-relative no-replace move；随后以`CancellationToken.None`从target no-follow重捕完整树，复核source identity与root结构字段、descendant exact metadata、目录项name/identity/kind完整inventory，并重跑reparse/hardlink/duplicate/busy-writer gate后重新持有，之后才允许Realm path-only publication。renamed root自身的rename-related change timestamps允许前进，不得据此放宽descendant metadata。该窄窗口不是filesystem transaction、全树排他或字节内容transaction；不得声称descendant handles跨move持有。任何可观察漂移均fail-closed，保留journal并冻结关联路径。
4k. rename恢复只以held-root physical `SourceOnly/TargetOnly`、expected source identity与同一record Realm source/target path组成可判定矩阵：`SourceOnly+source`为已回滚，`SourceOnly+target`回滚Realm path，`TargetOnly+source`前滚Realm path，`TargetOnly+target`为已提交。`Both/Neither/IdentityMismatch`、Realm冲突、record资格漂移或managed-root identity不一致一律歧义；可判定时只收敛同一记录path，不改展示/内容元数据，歧义时保留journal并冻结source/target。
4l. 成功rename推进全局selection generation并取消当时的pending selection，旧generation不得发布，但不销毁active immutable capsule；未来重新capture必须读取新managed path。scanner snapshot→commit、selection最终提交、rename与recovery共用coordinator；shutdown必须cancel并同步join worker后才释放Realm。operation/recovery输出只允许kind/phase/status/count，不得记录path、record/operation ID、physical identity或native异常正文。
4m. staged source在move前必须从held staging root做完整no-follow package capture，要求根`skin.ini`、publication plan中的closed allowlist实例类型、capsule revision、reparse、hardlink/duplicate identity、busy writer及完整inventory全部有效。capture还必须生成确定性完整physical-tree fingerprint：绑定capsule revision、每个节点identity/kind/length/creation/attributes/reparse/link/delete与目录层级中的exact ordinal name/identity/kind边界（包括空目录）；仅package root排除rename必然推进的last-write/change time，descendant时间仍须exact。最终preflight后只可释放NTFS move所必需的descendant handles，继续持有staging parent/source与managed root identity，不再观察caller cancellation，立即以held source/target parent执行同卷relative no-replace move；随后以`CancellationToken.None`从target重捕完整树。target root的rename-related时间可推进，但source→target identity、完整physical-tree fingerprint、content revision与全部安全gate必须与durable Prepared exact一致。任何SourceOnly/TargetOnly/Neither可判定结果返回前都必须重新枚举source/target两个name slot，按ordinal复核name/identity/kind，不能以较早枚举结果跨越cleanup或recapture边界。
4n. staged import的one-shot publisher只能在durable `FilesystemApplied`、最终target identity/capsule验证及最终Realm ID/path/owner冲突复核后执行。成功record唯一且必须为：`ID = operationId`、`FilesystemStoragePath = exact target managed path`、`Name`/`Creator`/hash来自最终capture、`InstantiationInfo`为现有closed allowlist、`Files`为空、非external、非protected、`DeletePending=false`；只有publisher完整满足scanner同等级注册/capsule/Realm冲突门后才可交接exact scanner owner，禁止通过直接写token伪造注册或与scanner竞争发布第二条record。
4o. staged-import recovery必须作为`SkinManagedFolderMutationRecovery`的同一按kind handler复用shared coordinator、canonical journal store与exact durable receipt：target exact且Realm absent时前滚发布，target exact且Realm为exact planned record时视为已提交；无论inspection已证明哪一终态，current forward recovery都必须从journal现有phase逐一持久化并exact reload`FilesystemApplied → RealmApplied → Committed`，每次checkpoint后重新枚举双槽并复核Realm，不能跳相位或在durable FilesystemApplied前调用publisher；phase write/load失败只留下最后durable journal，fresh restart继续且不得重复exact record publication。source exact、target absent且Realm absent/exact planned时，只删除exact planned record（若存在）、清理exact provisional source并回滚；neither且Realm absent/exact planned时，只有journal operation合同仍能证明staging是可丢弃OMS副本且外部原来源已保留才可按同样边界回滚。exact provisional cleanup必须以durable staging-root/source identity与树证明为authority，删除部分descendant后崩溃也要能在重启后只续删同一provisional root并最终得到双槽Neither；不得因完整capsule已无法重捕就扩大到外部原来源、managed target或foreign node。both、root/source/target identity mismatch、physical-tree/content drift、foreign/conflicting Realm、同ID/同path冲突或字段漂移一律保留journal并冻结target及相关operation状态。
4p. staged import不自动选择新record，不替换current active immutable capsule，也不得复用rename成功后的全局pending取消；无关pending selection在最终authoritative复核仍成立时继续。selection最终non-blocking admission失败后必须从coordinator取得exact active staged-import holder的typed completion；holder即使在失败后立即完成，既有completion object也必须可靠完成并驱动fresh retry，禁止再用外部running状态或全局boundary epoch猜测。generic mutation不得返回该retry authority；startup→staged-import连续争用可携同一preparation observation链式等待，但每次继续前及retry lease内都须复核generic mutation epoch。任何planned ID/path冲突必须在首个物理步骤前拒绝。首个物理步骤包括move或provisional cleanup，均要求Prepared durable exact reload；move一旦尝试，caller cancellation不再决定结果，由journal/recovery收口。startup scanner、rename、staged-import与selection retry worker须在Realm释放前统一cancel + synchronous join；operation/recovery状态只允许kind/phase/status/count，不得记录path、record/operation ID、physical identity或native异常正文。
4q. managed delete physical primitive必须由held managed-root与held exact source授权：tree capture必须用显式迭代walker并同时受capsule的entry/depth/resource-name预算约束，pending未处理节点也计入总预算，禁止递归栈溢出或先打开超预算句柄。final完整no-follow tree/authority/identity复核及caller取消检查后，首个外部物理步骤只能是held-root-relative no-replace move到operation-derived tombstone；之后不再观察caller cancellation。rename后的verification handles不具DELETE权，必须在验证其manifest与Prepared exact后释放，再从held root以fresh no-follow delete-exclusive handles重捕；这些handle必须持有DELETE但share access只允许READ，不能复用仍允许`FILE_SHARE_DELETE`的staged-import Provisional模式。same-session live重捕的完整树必须仍等于durable manifest；verification release→exclusive recapture窄窗内已经完成的node移出必须因exact mismatch在0次disposition时整轮拒绝，只有fresh recovery session的部分崩溃survivor tree可接受其子集。exclusive tree取得后再试图把已持有root/child移到sibling/authority外必须被sharing violation阻断。Windows directory share handle不等于namespace lock：重捕或final preflight前已可见的新增node、同名replacement或fingerprint漂移须在0次disposition时整轮拒绝；final preflight后竞态新增/replacement不得进入held delete list、不得被follow或删除，但允许已授权manifest节点部分清理后因root非空而失败，此时必须返回physical outcome uncertain，保留durable journal与exact Realm record，fresh recovery继续把foreign node判为歧义冻结。禁止把该边界描述为oplock、TxF或all-or-nothing tree delete。cleanup每一步继续拒绝reparse、hardlink、duplicate/identity/metadata/inventory漂移和source碰撞，不得触及foreign addition、同级目录、managed root或调用方path。成功physical state必须为source absent+tombstone absent；任何无法证明状态保留journal并冻结。
4r. managed delete recovery按source/tombstone/Realm exact fingerprint、source-node manifest、durable fallback disposition及phase幂等收敛：Prepared且source exact/tombstone absent/Realm exact可安全回滚，哪怕disposition尚为空；Prepared只有在disposition已durable确认、TargetOnly且Realm exact时才可先清理tombstone并写入FilesystemApplied，Prepared+Neither即使disposition已确认也属于首个checkpoint前来源异常消失，必须保持歧义。FilesystemApplied/RealmApplied才可按各自exact Realm/fallback证据从TargetOnly清理或Neither terminal physical state继续前滚；Realm只在durable FilesystemApplied后compare-remove exact record。raw/unconfirmed Prepared却出现TargetOnly/Neither、foreign/missing证据/字段漂移、Both、identity mismatch、source replacement或其它歧义均保留journal。`ProtectedPairCommitted`在RealmApplied/FilesystemApplied且source+tombstone均absent、record absent时仍须有与`OmsSkin.CreateInfo()`逐字段一致的exact protected fallback Realm record才可继续Committed；`NotRequired`明确表示operation线性化点的coherent noncurrent proof，恢复不得要求或创建fallback record，也不得重放UI打开确认框时的旧current快照。重启恢复不冒充重验旧runtime pair，current目标的coherent pair证明必须已在detach前完成。fallback scheduler callback与shutdown必须恰一方claim/reap pending completion，TCS完成先于可能重入的`SourceChanged`发布，late callback no-op；update thread不等待，shutdown在Realm释放前cancel并joinstartup scanner、rename、staged-import、delete、selection capture/retry全部真实worker。operation/recovery诊断只能输出kind/phase/status/count，禁止path、record/operation ID、identity或native异常正文。
4s. full managed-copy的唯一产品API是manager-owned `ImportManagedCopyAsync(externalRecordId, targetChildName)`等价surface；operation ID、staging path、worker与publication plan均由manager生成，UI不得传operation ID/path或直接调用既有`ImportManagedFolderAsync`。target child由用户明确输入并经过direct-child/NFC/Windows/collision验证，禁止从Name/path推导、merge、overwrite或自动suffix。fresh external capture/session必须成对产生同一revision的immutable capsule与bounded immutable logical-tree manifest；manifest保留ordinal relative name、file/directory kind、层级及显式empty directories。文件bytes只从exact capsule读取，directory只按paired manifest以destination handle-relative no-follow/no-replace创建，复制过程中不得重新按external path打开source；copy后须对logical manifest与capsule content revision双重exact recapture。创建`skin-mutation-staging/{operationId:N}`或写入任何destination byte本身就是首个filesystem步骤，不能发生在无durable owner时。C1只允许一个canonical v3 combined ManagedCopy intent/handler：首写前Prepared绑定operation-derived provisional/target slot、fresh authoritative external record fingerprint、exact capsule revision、held staging-root identity与有界排序logical intended manifest；phase显式覆盖Prepared/Copying/ProvisionalReady及既有move/publish语义，不创建第二个journal文件，也不从UI-visible stager切换到另一个operation。partial copy的每个可清理节点必须由durable proof识别为exact OMS provisional：取消、fault、crash或restart只删除已证明的planned subset；foreign addition/replacement、root/entry identity drift或证据不足保留intent并冻结，禁止按目录年龄、operation-like名称或启动清空staging猜测删除。caller cancellation只在首个destination write前决定结果，之后由journal/recovery收口。只有ProvisionalReady已durable write并exact reload、full manifest/capsule exact后，才可在同一intent内进入既有staged-import move/publish phases；startup先恢复该single canonical journal、再scanner，任何无法安全前滚/回滚的phase组合继续fail-closed。
4t. 普通`.osk`仍是hash-backed Realm package，不进入external/managed stager，也不改变noncurrent soft delete/default/export语义；legacy editor/update-import已按`3j`冻结。C1采用两层skin-scoped ingress：pre-open层在`ImportTask.GetReader`/`ZipArchive.Open`前只限制compressed source length并把非seekable stream写入bounded spool；随后必须由自身受compressed-input、central-directory bytes、entry count与内存预算约束的metadata parser/open立即枚举central-directory并检查name/type/declared size totals，不能先让通用reader无界物化metadata，再把后置检查称为gate。在任何`Filenames`向后续暴露、`GetStream`、fast hash、`Files.Add`、model construction或Realm publication之前完成其余准入。预算覆盖archive文件、总compressed/expanded、单entry、ratio、entry count、depth、raw与common-prefix-shortened path长度/分段/NFC/Windows case-fold、file-directory conflict、空名、traversal、ADS/device/trailing-dot-space、encrypted/symlink/Unix special、truncated/CRC、声明size溢出以及size为0/unknown/data-descriptor的bounded accept或typed reject；checked arithmetic与每个实际copy/hash stream继续用actual-byte硬计数和cancellation复核，不得信任header declared size或用runtime texture预算代替。`skininfo.json`的untrusted `InstantiationInfo`必须经过closed compatibility allowlist或稳定legacy fallback，禁止任意CLR type activation。拒绝/取消/失败不得产生`SkinInfo`、新增Realm file reference、仅由本次创建的orphan blob/temp，也不得误删共享content-addressed blob；temporary spool与file store回到基线，原`.osk`保留，成功才维持既有`ShouldDeleteArchive`语义。Shift-JIS、公共顶层目录、layout JSON、`Skin.InI`与历史有效modified classic/default/argon包保持回归。安全门优先skin-scoped；若修改共享`ZipArchiveReader`/`ImportTask`/`RealmArchiveModelImporter`，必须补beatmap及其它archive importer宽回归。C2 current reload另以fresh Realm declaration set、逐blob hash与规范capsule准备same-ID新revision；current ordinary delete先发布protected fallback并等待detach，再做Realm soft-delete，Realm失败须恢复exact旧pair/revision、record与blob。
4u. current v3已把“存在任一external即阻断全部managed mutation”收窄为exact-set局部碰撞：Rename/StagedImport/Delete/ManagedCopy在首个filesystem步骤前durable固化`ExternalCollisionDisposition`、registry generation/full digest与held ancestry non-overlap证明，并从proof取得、write+exact reload一直复验到final Realm collision linearization。disposition不得新增external绝对path、raw external record ID或raw physical identity；所有UI/diagnostic也不得输出path、record/operation ID、identity或native异常正文。register/unregister不得在该线性化窗口改变集合；fresh recovery必须重新枚举service-owned exact set并持有/比较physical proof，不能只信旧digest。v2按“pre-C1无service-owned external”的旧合同恢复且不得补写；v3缺disposition/digest/proof必须Invalid/freeze。journal支持UI只能动态inspect只读状态、稳定reason与脱敏bundle，并仅在fresh inspection唯一证明现有handler可安全forward/rollback时触发manager-owned async Retry；禁止raw journal、方向选择、删journal或解除冻结按钮。
4v. ordinary `.osk` 的transactional `RealmFileStore` receipt是按`RealmAccess + storage identity + content hash`分组的participant协议：同hash并发scope共用exact group，任一successful或unscoped writer观测到后不回滚。失败scope的每个group独立finalize，一组Realm/storage fault不得阻断其它group；失败group必须清除`Finalizing`并保留可重试receipt，generation guard阻止旧finalizer清理后来participant。record/blob baseline必须独立处置：只baseline Realm record不存在且fresh查询仍零usage时compare-remove record；只baseline blob不存在且该group实际写入blob时删blob。存在record/缺blob与缺record/存在blob两种非对称基线都必须回到原状，不得用单一ownership布尔值一起删除。`RealmArchiveModelImporter`/`SkinImporter`必须把cancellation token从scope贯穿input hash、existing blob hash、copy、metadata rewrite、Add/Replace与final transaction前后检查。锁序固定为Realm transaction → `import_scope_lock` → storage：不得在持有`import_scope_lock`时新建或等待Realm transaction，也不得从storage callback反向取该锁。Dispose在短锁内只采集待finalize group/generation，释放后逐组执行并独立捕获；Cleanup也只在短锁内取稳定门，释放后才做全局Realm/storage遍历。
5. schema 57 managed scanner只能维护owner为exact token `oms.skin.managed-folder.scanner.v1`、结构仍合法，且由scanner自身创建或由staged-import one-shot publisher通过scanner同等级注册门合法交接的记录；nullable opaque owner只是record metadata，不授予filesystem capability，null/unknown/foreign永不claim、改写、去重或清理。普通scanner不得消费publication plan或与publisher竞争创建第二条record。native source必须从同一held `chartskin` authority handle产生完整direct-child inventory并分离`ObservedPaths`/`ValidDiscoveries`：file/reparse/坏包/忙写包只要名字合法仍属observed，不能因未valid而负向删除；单个observed-invalid只保护其exact path，只要root inventory整体仍完整稳定，不阻止清理其它确实absent的唯一exact-own记录。只有完整稳定scan可在单一Realm事务中新增、更新、revive或soft-delete；同路径冲突、根缺失/不可读、inventory不稳定、非法snapshot、取消、异常，或任何导致root inventory无法完整稳定收口的失败轮次均不得提交negative cleanup，取消在commit线性化点前须回滚整笔事务。soft-delete只改Realm `DeletePending`，不得删除磁盘。启动worker必须在同一typed startup sequence边界内先幂等恢复、后scanner，不得依赖update scheduler；`OsuGame.Dispose`必须在base/Realm释放前统一cancel+join startup、rename、staged-import、managed delete、selection/reload capture/retry、materializer/work fence与retire worker。scanner始终只运行启动reconcile，不是watcher；启动后新增目录仍需重启发现，已登记current记录的原位内容只经Settings manual Reload进入新revision。
6. folder-backed `SkinInfo.Files` 保持空；external `NativeStorage` 或 managed source 只用于 native no-follow capture，不得作为运行实例持续直读的 live store。资源必须从同一完整 capsule 读取；非 folder、普通 `.osk` 与 Oms 路径保持既有行为。
7. C2只关闭当前production consumer所需的same-ID package revision publication：按`3m`把三源的新instance/current package资源整体准备并一次发布，失败保留旧instance且只释放provisional owner，成功后旧owner等待participant/work/operation lease全部detach。C3～C6新增layout/codec/scene/script consumer必须同切加入同一协议；只有到C6把ini/manifest/scene/script/全部素材共同纳入后，才可称最终整包reload门关闭。
8. 删除current skin前必须通过与manual Reload相同的revision transaction发布已验证protected fallback并等待旧revision detach，不能只向scheduler排队selection pair后立即mutation。canonical接管前fallback authority为程序化`OmsSkin`，C7后改为只读`oms-simple.osk`。current managed Delete随后才进入C1 journal/physical/hard-remove与recovery；首个physical后的uncertain failure保持fallback并由durable recovery收口，不承诺恢复旧source。current external Unregister随后只fresh compare exact service-owner/record/current revision并pure-Realm remove，任何失败恢复旧revision且source零I/O。current ordinary `.osk`随后只Realm soft-delete，Realm失败恢复旧pair/revision、record/blob。非current目标可按既有各自mutation合同不改变protected pair；external永远不能被写、改名或物理删除。
9. schema 56只读清点已经发现的失效`InstantiationInfo`不得依赖`SkinInfo.CreateInstance()`的历史`TrianglesSkin` fallback静默吞掉，也不得靠普通启动只修fixed-ID protected记录后宣称迁移完成。Realm/hash-backed已导入包必须先保全内容，再按用户批准的定点方案重导入、保留或移除；scanner不得代办该清理。Realm写事务可能不更新文件mtime，迁移证据必须同时验证SHA-256与动态schema状态。

## HUD 与既有迁移约束

1. 在 scene/event ABI 接管前，不得破坏现有 `IBmsHudLayoutDisplay.SetComponents(wrapped HUD, gauge, combo)` 签名；需要扩展时使用 versioned optional contract。
2. BMS 默认 gauge 在迁移期继续免疫通用 `HUDOverlay.ShowHealthBar`，保持当前可玩信息不因 NoFail 等通用开关消失。
3. wrapped HUD 中重复的 upstream combo 和 offline leaderboard 继续在装配树中移除，不得只以 `Alpha=0` 隐藏。
4. 新 feedback 不得通过遍历 wrapped HUD、偷改 gauge/combo 或 ad-hoc toast 接入；应发布 neutral event/state，由 scene/script 消费。
5. pre-start lane preview 若重启，仍属于 playfield/lane slot，不属于 HUD/toast；第一轨解释为第一非 scratch 普通轨。
6. 当前 strip 必须显式覆盖 `ComboCounter`、`LegacyDefaultComboCounter` 和 `DrawableGameplayLeaderboard`；`LegacyDefaultComboCounter` 不是 `ComboCounter` 子类，测试不得只匹配前者。
7. 当前 `BmsGaugeBar : HealthDisplay` 必须在真实 `HUDOverlay` 下压过 `ShowHealthBar=false` 的淡出并保持可见；裸 dependency container 不能作为该路径的唯一测试。

## 既有 LN 视觉状态合同

1. 在 neutral event adapter 完成前，长条 body 状态继续由 `DrawableBmsHoldNote.BodyState : IBindable<BmsLongNoteBodyState>` 暴露，状态 `Idle/Holding/Broken` 只由 hold gameplay truth 派生；managed/default body 共用的视觉宿主不得自行读取判定内部。
2. 当前兼容默认值保持：body width `0.5775`；`Idle==Holding` 使用 head 色、alpha `0.8`；`Broken` 去色并使用 alpha `0.32`；状态改变的 tint/fade 为 `80ms`，异步首次挂载立即投影当前态；tail 仍 `Alpha=0`。修改须同步 `BmsSkinTransformerTest` 与真实 hold 产品 fixture。
3. `Broken → recover` 只允许 HCN；CN 中途松开不可接回，语义 authority 见 [P1-E 约束](../P1-E/TECHNICAL_CONSTRAINTS.md)。Skin V1 event adapter 只能投影该状态，不得重新解释 LN/CN/HCN 规则。

## 测试与发布约束

1. 验证按实际改动面分层：仅改 BMS ruleset 内组件且未触碰 shared skin、mania compatibility 或 fallback authority 时，至少覆盖用户 package、对应 `Provide/Inherit`、BMS relevant/full 与 Release；修改 shared skin、mania compatibility 或 fallback authority 时，必须追加 core skin focused、mania relevant、BMS relevant 与 Release。`Suppress`、真实 `oms-simple`、canonical 恢复与双 ruleset 包只在对应实现存在后作为 gate，不能用尚不存在的产品面制造伪通过；迁移期间保留当前 `OmsSkin` 对照，直到文件 fallback 正式接管。
2. parser/type assertion 不能替代真实 `SkinManager`、选择链、folder authority、event order 和生产 host 测试。
3. layout 最低矩阵：5K/7K × 四 style，9K BMS，9K PMS，14K；每格覆盖 lane order/bounds/scratch role/BGA viewport/gauge safe slot/时序不变。
4. sandbox 最低矩阵：权限拒绝、无限循环、内存/节点超限、异常熔断、replay determinism、seek/retry/pause/reload 和 profiler。
5. V1 release 必须有两个普通 `.osk` 公共 API 验收包：`oms-simple` 与 `oms-complex`，两者均同时包含 mania/BMS。`oms-simple` 不得被补出已显式 suppress 的可选件；`oms-complex` 不得使用私有接口。
6. 每次记录本次实际运行的 BMS/mania/core/Release 与人工视觉/性能结果；按上条未要求重跑的套件也要明确写“未运行及原因”。已知失败必须稳定归因，不得把既有失败写成新回归。
7. 文档不得把“代码 provider 可替换”“ini 可配置”“scene 可声明”“script 可编程”混成一个完成状态；能力矩阵必须分列。
8. 不得宣称 LR2/beatoraja/IIDX 文件格式兼容；“接近 IIDX 表现上限”只描述公开接口表达力。
9. 已导入 `.osk` 的BMS普通短键编号帧动画与长条head/body/tail静态图/编号帧动画必须分别进入集中用户视觉验收清单，并在Skin V1/release宣称完成前取得确认；2026-07-14的静态恢复验收只能证明`SV1-0`基线，不得复用。待签收本身不阻塞后续自动可验证切片，但未签收不得称产品交付、`SV1-1`完成或release gate通过。
10. ordinary note 与 long-note head/body/tail 的首个产品纵切闭合后，剩余 optional slot 不再通过私有逐件 C# provider/display 扩张；它们必须等待 shared scene/runtime 接管并服从对应 ABI、budget 与 fallback gate。
