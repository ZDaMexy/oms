# P1-J 开发进度：BMS gameplay runtime 性能与音频时序治理

> 最后更新：2026-06-21。**本轮（原生 BMS 键音保真两改，构建+全测通过、用户实机实测确认 ✅）**：按用户两条判据审查 autoplay 键音链路——**(A) autoplay 必须等同 100% 完美游玩**、**(B) 「键音通道数」是否好设计/是否该智能自动**。落地：① **autoplay = 完美游玩**：确诊 autoplay 每音符**双触发**（音符 `AutoPlay=true` 退出输入 → replay 合成按键直达 `BmsLane` → lane armed 键音叠音符 auto-apply 键音；per-WAV cut 多数掩盖、armed≠音符槽或异步通道状态时露馅重复/发错），修复 = `BmsLane` 在本 lane 有自动音符时抑制 armed 键音、发声交给音符（CONSTRAINTS 新增 **#3b**）；连带结论：32 通道截断 + 同步触发抖动**非 autoplay 专属、玩家完美游玩同样存在**。② **键音池固定上限 32 → 自动增长**：`getNextChannel` 饱和不再轮转偷断，改为**新增通道（封顶 256）、仅 256 仍饱和才偷**——保真单调、自然有界、消「截音」，`KeysoundConcurrentChannels` 降级为「起始/常驻基线」（#4/#8/产品#2 重写、tooltip 同步）。验证：Release 0 错 + **BMS 936/936**（改写 `TestSharedKeysoundStoreSingleSamplePathRotatesBuffers` + 新增 grow/autoplay-suppress 两测）。**实机听感（对照 beatoraja）用户实测确认优化明显、暂无异常 ✅（2026-06-21）。** 详见 [CHANGELOG](CHANGELOG.md) 2026-06-21。
> 此前 2026-06-11。**（J6 游玩期性能，两项确诊修复 + 用户实测 ✅）**：① 转谱游玩「越后越抖/按键挂钩/规律一顿一顿」真因 = **每键音触发 sample-drawable 重建 churn → gen1 晋升风暴**（探针三轮取证 + 代码审查；作废「疑渲染/通用高帧 GC」旧叙事），修复 = store 通道**同样本快路径**（同槽重触发零重建、cut 语义不变）——用户同谱实测密集区 maxFrame 15–30ms→5–10ms、「基本全程稳定」✅；② 偶发 **~220ms 冻结** = 开局段阻塞 gen2 全量 GC（玩家模式无预热、362 个 WAV 游玩中冷解码顶爆 gen2 预算），修复 = **keysound prewarm 放开到玩家模式**（BMS 原生 + 转谱两侧对等，对齐 LR2/beatoraja 全量预载；CONSTRAINTS #7 重写）——**用户同谱回归实测 ✅：游玩中 stalls=0、阻塞 gen2=0（上轮 3 次 ~220ms → 0）、maxFrame 2–12ms @ ~1000fps，判定「合格」**。探针 `BmsGameplayStallDiagnostics`（store 子节点、真实游玩才激活、stall/gen2/2s 心跳才写日志）经用户确认**留作长期诊断 seam**。验证：Release 0 错 + 焦点 78/78 + BMS 871/871×2 + mania 22/22。详见 [CHANGELOG](CHANGELOG.md) 2026-06-11。
> 此前 2026-06-10。**性能优化（J6）**：转谱 **tap KEY note 从非池化改为池化**——经 mania 自有 `IManiaKeysoundStore`/`IHasManiaKeysound` 接口让池化 `DrawableNote.PlaySamples` 路由 shared store（删 `DrawableBmsConvertedKeyNote` 及其工厂分支），消除「每 tap 一个常驻非池化 drawable」的加载/内存/GC 代价、键音语义零改动；同轮经 `IHasManiaKeysound.KeysoundSample` 补回 BGM/scratch 的 autoplay 预热缺口（bgm1 置空 `Samples` 后丢失）。验证：`TestSceneBmsToManiaKeyNoteStoreRouting` **2/2**、转谱器+mania autoplay **22/22**、BMS **871/871**、mania 778 通过（4 个 `TestSceneAutoGeneration` HoldNote 失败已 stash 对照确认为**既有失败**、非本轮引入）、Release 0 错。明确后置：BGM/scratch 调度器化、store 128 floor 下调（会回归长 BGM 偷断修复）、稳态高密段渲染——均须先 profile。
> 此前 2026-06-08 **当前态**：转谱-mania「按 key1 触发 bgm1 / 长音重叠 / 暂停不停」**已解决**（真因 = mania 按键音效反馈 `GameplaySampleTriggerSource` 播了转谱 BGM/scratch 对象 `Samples` 里的键音；修复 = 转谱器把这些对象的 `Samples` 置空、键音改走 store 的 `KeysoundSample`；用户多谱实测 ✅）。J6 转谱音频已闭合 **E（暂停停 BGM）/ 长 BGM 通道偷断（floor 128）/ tap-note→store per-WAV cut（生产默认，现已池化）**。
> 逐日调查史（06-06 两次回退、06-07 真因突破+Track 泄漏修复回退、06-07b 移交、06-08 解决）见 [CHANGELOG.md](CHANGELOG.md)；权威约束见 [TECHNICAL_CONSTRAINTS.md](TECHNICAL_CONSTRAINTS.md) #10 / #11；bug 复盘见记忆 `reference_bms_bgm1_pause_keytrigger_bug`。
> 主线全局状态见 [../../mainline/DEVELOPMENT_STATUS.md](../../mainline/DEVELOPMENT_STATUS.md)。本文件只记录 `P1-J` 的真实进展。

## 当前阶段

- **阶段定位**：`P1-J` 已从 planning-only 进入首轮代码落地；`J1` 已完成，`J2` / `J3` 第一刀已完成，`J4` 已完成。**早先的「once-per-run 单次致命卡顿」线索已于 2026-06-11 确诊收口**——其转谱-mania 形态 = 开局段阻塞 gen2 全量 GC（游玩中键音冷解码所致），随 prewarm 放开玩家模式修复并经用户实测（游玩中 stalls=0）；当前重点转为 J6 剩余项（转谱 LN 键音走 store、50k 极端 dense profile）与 `J5` 后置人工验收。core replay fast-forward 已确认不再继续；full autoplay 保持 BMS 专用分流。
- **代码状态**：shared `BmsKeysoundStore` 继续作为 BGM / note / LN / lane replay 的统一播放池，但 gameplay keysound 已不再无条件延后到后续 scheduler tick；`BmsLane.shouldTriggerEmptyPoor()` 与 `BmsOrderedHitPolicy.getParticipatingHitObjects()` 已去掉首批热路径对象物化，`DrawableBmsHitObject.PlaySamples()` 已收口到单样本 keysound 路径，`KeysoundConcurrentChannels` live 改值也已从 rebuild-all 改为 grow-immediately / shrink-deferred 的 non-destructive resize；同轮还补上 pause / seek 生命周期回收，shared store 现会在 gameplay 暂停与 seek 时主动停掉活跃频道，避免长 one-shot BGM / keysound 样本穿透暂停边界。其后续热路径 follow-up 也已落地：`BmsLane` 不再在玩家命中后通过 `NewResult` 重复触发 ordered-hit 扫描，empty-poor 检查已去掉 per-press `HashSet` 分配，shared store 单样本入口也已改成 channel-local 双缓冲，不再为每次单样本播放临时 new 单元素数组；`DrawableBmsHoldNote.resolveBodyTicksUpToCurrentTime()` 也已改成按时间有序的 early-break，不再在每帧把整条长键后续 body tick 全量白扫。dense autoplay 的 replay 链最近保留了两条 BMS-side 优化：一是 `BmsFramedReplayInputHandler` 继续复用 `BmsReplayFrame` 缓存的 lane-action 掩码与列表，去掉每帧 `Any/Where/ToList`；二是 `DrawableBmsRuleset` 现对 full autoplay 额外分流到“对象级 `AutoPlay` + `BmsAutoplayReplayInputHandler` 直接按当前时间采样输入状态”的专用路径，不再让 dense full autoplay 继续依赖逐 replay frame 边界驱动判定本身。keysound 预热（core `Playfield` 只预建 `hitObject.Samples` / `AuxiliarySamples`，BMS keysound 走 store 专用 sample，须 owner-side 在 `LoadComplete()` 预热）最初仅 gate 给 full autoplay；**2026-06-11 起对玩家模式一律执行**（游玩中冷解码实测触发 ~220ms 阻塞 gen2 冻结，见 CHANGELOG 2026-06-11）。2026-05-30 的键音链路审查又落地三处修复：(1) `BmsKeysoundStore.getNextChannel()` 从纯 round-robin 改为 **idle-first**——仍有空闲通道时优先复用，只有真正复音饱和才按轮转偷取，修掉"低于复音上限就提前截断长样本（尤其 layered BGM 长 sustain）"的原始缺陷；空闲集由 `reclaimIdleChannels()` 每帧重建（O(N) 读、无分配、`getNextChannel` 仍 O(1)），不回退 dense-chart 热路径。(2) shrink 裁剪通道改为 `Retired` 标记 + `Remove(channel, true)` 真 dispose，不再留下脱挂未释放的 sound drawable。(3) pressed-POOR/miss（含 LN head）现在在 key-down 经 `DrawableBmsHitObject.PlayKeysoundFromPress()` 补播该 note 的 keysound，修掉"按了键判 POOR 却完全静音、偏离 IIDX/LR2/beatoraja"的语义缺口；clean hit 仍只走 `PlaySamples`、不 double。(4) 同日新增 **per-WAV cut**（2026-05-31 由文件名归组改为按 **WAV 槽号 `KeysoundId`** 归组）：`BmsKeysoundStore` 以 `activeSampleChannels`（键=槽号）+ 通道 `CurrentCutGroup` 追踪在播槽，同一槽仍发声时被再次触发会复用该通道干净重启（掐断前一实例），而非叠加副本；**不同槽即使同文件也不合并**（允许"多槽同文件自重叠" hi-hat/拍手，对齐 LR2/beatoraja）。2026-05-31 另落地：**LN tail 一律不发声**（`DrawableBmsHoldNoteTail.PlaySamples()` 空实现，只头发声），并配合 P1-K 修复缺省 `#LNTYPE`（长条解析恢复）——三者闭合 GOODBOUNCE [A] 的"少键 + scratch 人声截断 + fee feet"链。
- **验证状态**：`BmsDrawableRulesetTest` 已在每个切片后持续通过；`J4` 现已同时具备 shared-store shrink 回归与 `config -> drawable ruleset -> playfield` direct binding coverage，其中 `TestSceneBmsKeysoundChannelConfigBinding` **3/3** 已通过；`J5` 侧已同时补上 dedicated `BmsOrderedHitPolicyTest` **2/2**、`TestSceneBmsSharedKeysoundTiming` **3/3**、`TestSceneBmsKeysoundPlaybackLifecycle` **3/3** 与新的 player-level `TestSceneBmsPlayerAudioSemantics` **3/3**，把 scratch stream ordered-hit、shared-store same-frame timing、pause / seek 生命周期回收，以及 seek 回放后的 BGM event 重播语义都从大回归文件里独立出来；本轮还补上 `TestSharedKeysoundStoreSingleSamplePathRotatesBuffers` 并回跑 ordered-hit / empty-poor / shared-store 相关 focused suite **11/11**。针对 dense autoplay / replay 的最新 focused validation 当前基线为：在撤回 core skip-fast-forward 后，`FramedReplayInputHandlerTest` **9/9** 通过，BMS replay focused suite `BmsReplayFrameTest|TestSceneBmsReplayStability|TestSceneBmsReplayRecording` **7/7** 通过；新增 `TestSceneBmsAutoplayReplayPlayback` 也已证明 full autoplay 专用路径仍会完成回放、保持非忽略判定全 `Perfect`，并继续驱动 key counter，focused suite **3/3** 通过；相邻 combined replay/autoplay suite **11/11** 通过。最新补上的 autoplay keysound 预热改动也已通过 autoplay-focused **4/4** 与 keysound-neighbour **9/9**。2026-05-30 的键音链路修复又新增两个判别性 focused 用例：`BmsDrawableRulesetTest.TestSharedKeysoundStorePrefersIdleChannelOverBusyOne`（空闲通道存在时不得偷取繁忙通道）与 `TestSceneBmsSharedKeysoundTiming.TestPoorPressStillTriggersKeysound`（pressed-POOR 仍向 shared store 发起 keysound 请求），后者把 `TestSceneBmsSharedKeysoundTiming` 提升到 **4/4**；per-WAV cut 再加 `BmsDrawableRulesetTest.TestSharedKeysoundStoreCutsSameSampleRetrigger`（同一 WAV 重触发只占一个通道）。2026-05-31 的 LN tail 静音再加 `TestSceneBmsSharedKeysoundTiming.TestHoldNoteTailKeysoundStaysSilentWhileHeadSounds`（提升到 **5/5**）。完整 `osu.Game.Rulesets.Bms.Tests` 的最近全量快照为 **936/936**（Release，2026-06-21；含本轮 autoplay=完美游玩 + 键音池自动增长两改的新增/改写用例）。
- **文档状态**：`P1-J` 四件套与主线文档现已同步到“首轮代码已开始落地”的口径，不再停留在仅规划状态。

## 已确认事实

- `BmsPlayfield.KeysoundStore` 是当前 shared keysound pool owner；`DrawableBmsHitObject` 和 `BmsLane` 都通过它播放样本。
- `BmsKeysoundStore.Play()` 已移除 gameplay keysound 的无条件 `Schedule()`；当前保留 `IEnumerable`、数组与单样本三个入口，其中数组路径可直接复用上游已 materialize 的样本数组。
- `BmsLane.shouldTriggerEmptyPoor()` 现已改为单次遍历候选，不再先 `ToArray()`；`BmsOrderedHitPolicy.getParticipatingHitObjects()` 也已改为 alive-first 的流式枚举，不再为判空而先物化整组对象。
- `DrawableBmsHitObject.PlaySamples()` 已收口为单样本 keysound 路径，不再对单个 keysound 做 `Cast().ToArray()`；`BmsLane.playCurrentLaneKeysound()` 也已接到 shared store 的单样本入口。
- `BmsLane` 现已移除玩家命中后的重复 ordered-hit `HandleHit()` 调用；当前 player-hit 路径只在 `DrawableBmsHitObject.OnUserPressedSuccessfully` 上触发一次 ordered-hit 扫描。
- `BmsLane.shouldTriggerEmptyPoor()` 现已去掉 per-press `HashSet` 去重分配；这段逻辑只做布尔 OR，不依赖唯一计数，重复候选不会改变最终判定。
- `BmsKeysoundStore` 的单样本入口现已改成 channel-local 双缓冲；在继续遵守 `SkinnableSound.Samples` array contract 的前提下，shared store 不再为每次单样本播放临时构造新的单元素数组。
- `BmsHoldNote.CreateNestedHitObjects()` 会按时间顺序生成 `BodyTicks`；因此 `DrawableBmsHoldNote.resolveBodyTicksUpToCurrentTime()` 现在可以在遇到首个 future tick 时直接 `break`，避免 HCN/CN/LN 长键在每帧重复扫描整条后半段 body tick 列表。
- dense autoplay 当前确认仍走 `ReplayPlayer -> FrameStabilityContainer -> FramedReplayInputHandler` 链，而不是玩家手动输入链；因此高压 autoplay 掉帧时，replay catch-up 与 replay-state 分配本身也是可优化的 owner surface。
- non-frame-accurate replay 过去虽然不是“每帧全表扫描”，但在 frame-stable playback 下，`FramedReplayInputHandler.SetFrameFromTime()` 仍承担着“每次调用最多推进一个 replay frame 边界”的合同，供外层 catch-up 循环逐步喂入中间输入状态。dense autoplay 的人工压测已经证明，若在这里单次跨过多个 frame，会直接转化成 autoplay miss，而不是纯性能收益。
- `BmsReplayFrame` 现已缓存 lane-action mask 与 lane-only action list；`BmsFramedReplayInputHandler` 不再每帧做 `Any/Where/ToList` 来判断重要区间和构造 pressed actions。
- `DrawableBmsRuleset` 现已把 full autoplay 与普通 replay 区分开：full autoplay 会给 `BmsHitObject` 设置对象级 `AutoPlay`，并改走 `BmsAutoplayReplayInputHandler` 的 direct-time 输入采样；普通 replay 仍保留既有 `BmsFramedReplayInputHandler` 与逐 replay frame 边界推进合同。
- core `Playfield` 虽然会在 `OnHitObjectAdded()` 时预建 `hitObject.Samples` / `AuxiliarySamples` 的 sample pool，但 BMS gameplay keysound 走的是 `BmsKeysoundStore` 专用 `BmsKeysoundSampleInfo` 路径，并不自动吃这条通用预热；因此 dense full autoplay 的单次卡顿补丁当前落在 `DrawableBmsRuleset.LoadComplete() -> BmsPlayfield.PrewarmKeysounds()` 这条 BMS owner 链，而不是继续改 core replay。
- 键音池基线 `DEFAULT_CONCURRENT_CHANNELS = 32` 现为**硬编码默认、不再有 UI 设置**（2026-06-22 删除「键音通道数（基线）」选项，详见 [CHANGELOG.md](CHANGELOG.md) 2026-06-22）：自动增长已让基线无需手调，原生 `BmsPlayfield` 以 `new BmsKeysoundStore()`（基线 32）创建、转谱 `BmsToManiaKeysoundStoreFactory` 楼底 128。基线语义仍为**起始/常驻而非硬上限**：池在饱和时自动增长（见下条）。`BmsKeysoundStore.ConcurrentChannels` 的 non-destructive resize 语义仍在（调高立即扩容、调低延后到超额 channel 停播后再裁剪、自动增长出的通道不随常规播放回收），现仅在构造时由 factory 设楼底使用，不再有运行时 config 改值入口。
- `BmsKeysoundStore.getNextChannel()` 现为 **idle-first + 饱和自动增长**（2026-06-21 由「饱和即偷取」改）：先从 `freeChannels`（每帧由 `reclaimIdleChannels()` 重建、(re)size 时播种的空闲集）取空闲通道；**全部繁忙（复音饱和）时新增一个通道（封顶 `MAX_CONCURRENT_CHANNELS`=256）而非偷断仍在播的样本**，仅在已达 256 仍饱和时才按 `nextChannelIndex` 轮转偷取。这在「idle-first 修低于上限就偷断」之上进一步消除「达上限就截音」——增长对保真单调（只补不截）、自然有界于该谱真实峰值复音（≤ 同时发声不同槽数）。`getNextChannel` 仍 O(1)（空集即判定饱和→增长、不扫描全池），每帧重建 O(N) 读无分配；增长复用 live 调高基线的运行时建通道路径。
- shrink 裁剪通道现在会先置 `BmsKeysoundChannel.Retired = true` 再 `channels.Remove(channel, true)` 真 dispose；`getNextChannel` / 重建均跳过 retired 引用，因此既修掉了反复 live resize 留下的未释放脱挂 sound drawable，也不会复用已裁剪通道。
- 玩家按键的 keysound 现遵循"key-down 必出声"：clean hit 仍走 `DrawableBmsHitObject.PlaySamples()`（Hit 状态），而被消费的 pressed-POOR/miss（普通 note 在 `OnPressed`、LN head 在 `TryApplyHeadPress`）改为在 key-down 经 `PlayKeysoundFromPress()` 补播该 note keysound；未按键的自然 miss 与 tail release miss 仍静音，BGM / autoplay 不变，clean hit 不 double。
- **LN tail 一律不发声**：`DrawableBmsHoldNoteTail.PlaySamples()` 已重写为空（含 release / autoplay）。LNTYPE1 长条尾对象常重复头 WAV，播放会与头叠 double（实测 GOODBOUNCE scratch 长条 "stomp your fee feet"），叠加 per-WAV cut 还会掐断头；对齐 LR2/beatoraja「长条只头发声」。尾 `TailKeysoundSample` 仍保留供空击 armed 时间线（`BmsBeatmap.LaneKeysoundTimelines`），只是不再 auto-play。
- `BmsKeysoundStore` 现实现 **per-WAV cut，按 WAV 槽号（`KeysoundId`）归组**：`activeSampleChannels`（`Dictionary<int, BmsKeysoundChannel>`）记录每个槽当前所在通道，`getChannelForCutGroup(int)` 在"该通道仍 busy 且 `CurrentCutGroup == cutGroup`"时复用之 → 同一槽重触发干净重启（掐前一实例）。**不按文件名归组**：不同槽即使同文件也不合并，所以"多槽同文件做自重叠"（hi-hat/拍手）能正常并发（对齐 LR2/beatoraja）。槽号经 `Play(sample, balance, int cutGroup)` 传入：note/head/BGM 用 `KeysoundId`、空击 armed 用 `BmsLaneKeysoundEntry.KeysoundId`；无槽入口（2 参 `Play` / 数组）不参与 cut（`CurrentCutGroup = null`）。同一槽被重复排布时后一次必掐前一次，属预期。
- `PausableSkinnableSound` 默认只会在 sample-disable 时立即停掉 looping 样本；为避免长 one-shot `BmsBgmEvent` / keysound 样本穿透 pause / seek，`BmsKeysoundStore` 现额外监听 `GameplayClockContainer.IsPaused` 与 `OnSeek`，并在边界处统一 `StopAllPlayback()`。
- `TestSceneBmsPlayerAudioSemantics` 现已锁住两条更接近玩家观察面的合同：`GameplayClockContainer` 在 pause / resume 间会持位并从原位置继续，且 `BmsBgmEvent` 在 seek 回事件之前后会清掉旧请求，并在再次跨过事件时间时重新向 shared store 发起播放请求。
- 键音通道数的 config→store live binding 与 `BmsSettingsSubsection` 的相关提示文案已于 2026-06-22 随设置项一并删除（`DrawableBmsRuleset` 不再绑定该 config，对应的 `TestSceneBmsKeysoundChannelConfigBinding` 也已删除）；store 的 non-destructive resize 行为本身保留（见上条），只是不再由用户配置驱动。
- `BmsDrawableRulesetTest` 现已锁住 late-empty-poor 行为；`P1-J` 任何优化都必须以“不回归这些语义”为前提。
- **`BMS -> mania` 转谱音频（J6）当前态：E / 长 BGM 偷断 / tap-note per-WAV cut / bgm1 按键触发 四处已闭合**（逐日过程含两次回退见 [CHANGELOG](CHANGELOG.md) 2026-06-01~06-08，下为现状）：
  - 转谱 BGM/scratch/tap-note 统一走**复用的 `BmsKeysoundStore`**——`DrawableManiaRuleset` 反射 `BmsToManiaKeysoundStoreFactory` 创建、`CreateChildDependencies` **同时** `Cache`（runtime 类型 `BmsKeysoundStore`，供 BGM/scratch 非池化 drawable 解析）与 `CacheAs<IManiaKeysoundStore>`（mania 自有接口，供池化 `DrawableNote` 解析、不需引用 BMS 程序集）、`load` 里 `AddInternal` 解析 `GameplayClockContainer`；**tap KEY note 走池化 `DrawableNote`**，其 `PlaySamples` 经 `IHasManiaKeysound` 调 `IManiaKeysoundStore.Play(sample, balance, cutGroup)`、无 store/无键音回退 `base.PlaySamples`；**BGM/scratch 仍各自非池化 drawable**（`[Resolved(CanBeNull=true)] BmsKeysoundStore` 后 `Play(...)`）；暂停/seek 由 store `StopAllPlayback` 统一停。
  - **E（暂停停 BGM）已修 ✅**（一次性样本不随暂停停，必须中心化 pause-aware store；2026-06-01 用户实测）。
  - **长 BGM 被 32 通道饱和轮转偷断已修 ✅**：长 BGM（如 `bgm1`，整首单次触发、占通道最久）在并发峰值 27–36>32 时被 `getNextChannel` 偷取掐断；修复 = 转谱 store `ConcurrentChannels = Math.Max(config, 128)`（floor 远超峰值 → 永不被偷）。治标近似，治本（长样本不进可偷池 / 分池）碰 #8 红线、后置。
  - **tap-note→store 已转正生产默认且已池化（2026-06-10）**：`case BmsHitObject` 发 `BmsConvertedKeyNoteHitObject`（`Note` 子类、带 `KeysoundSample`/`KeysoundId`、实现 `IHasManiaKeysound`）、`ShouldHost` 含 KEY 音符 → per-WAV cut 跨 BGM↔KEY + KEY↔KEY(tap) 统一生效，转谱键音重复的 **tap-note 部分已修**。✅ **此前 🔴「非池化 perf」已解**：转谱 drawable 工厂不再认领 KEY note → `CreateDrawableRepresentation` 返回 null → 走 mania `Note` 池（框架 `prepareDrawableHitObjectPool` 基类型回退）发**池化 `DrawableNote`**，不再每 tap 一个常驻非池化 drawable（消除加载期构造 + 内存常驻 + GC 大堆代价）；键音经接口路由 store，语义零改动（per-WAV cut 不变）。删 `DrawableBmsConvertedKeyNote`。这正是 pooling-preserving 正解，未新长 per-note sample player；回归守护 `TestSceneBmsToManiaKeyNoteStoreRouting` 2/2。
  - **bgm1 按键触发 bug 已修 ✅**（2026-06-08，用户多谱实测）：转谱器把 BGM/scratch sample-only 对象的 `Samples` 置空（键音只放 `KeysoundSample` 经 store 自动发声）；真因 = mania 按键音效反馈 `GameplaySampleTriggerSource`（`Column.OnPressed` 每次按键播本列下一个对象的 `Samples`），与 store/per-WAV cut 无关。见 CONSTRAINTS #11 + 记忆 `reference_bms_bgm1_pause_keytrigger_bug`。
  - 同日另落地两条对的改进（保留）：mania 转谱 keysound prewarm（`prewarmConvertedKeysounds`，gate=converted-BMS + `ModAutoplay`，对齐 BMS 原生 `PrewarmKeysounds`；BGM/scratch `Samples` 已置空 → 当时不再被该预热覆盖）+ store 通道接 `KeysoundConcurrentChannels`（`Create(IRulesetConfigCache?)`）。**2026-06-10 补回该预热缺口**：`prewarmConvertedKeysounds` 现额外按 `IHasManiaKeysound.KeysoundSample` 预热（BGM/scratch/KEY 全覆盖，BGM/scratch 仅此一途，因其 `Samples` 空），修掉 bgm1 修复后 autoplay 下 BGM/scratch 首播冷解码抖动；过时注释同步修正。**2026-06-11 起预热更对玩家模式一律执行**（开局 gen2 冻结修复，见 CONSTRAINTS #7）。
- **仍开放遗留（均与已闭合的 bgm1 bug 无关）**：① 转谱键音重复的 **LN 部分**——须**池化嵌套头**（2026-06-06 非池化自定义嵌套 head → `DrawableHoldNote.Update()→Head` 空容器必崩，已回退；再尝试前须先有 player-level 集成测试 + 运行时日志 harness，已落地）；② **长 BGM resume 截断**（暂停恢复续播须时钟驱动 Track 保位，一次性 store 样本做不到）；③ **dense D 卡顿**（共享 store 已排除"音频对象数"为主因；其「drawable 数量」中**每 tap 一个常驻非池化 drawable**这一加载/内存/GC 贡献项已随 tap 改池化消除〔2026-06-10〕，但**稳态高密段渲染/更新预算**与 **BGM/scratch 非池化每帧开销**、转谱 store 128 通道每帧扫描仍待 profile、后置）；④ **选歌预览 Track 泄漏进游玩**——有 master AudioFile 的谱面 `working.Track` 是真实音频，BMS 原生（解耦时钟不停源 Track）与转谱-mania（时钟驱动 Track 叠键音）两路径都没静音它；`StopUsingBeatmapClock` 修复尝试在真实耦合时钟下无效、已回退（虚拟轨单测假阳性），须真实 app 取证再重做。

## 进度矩阵

| 事项 | 状态 | 备注 |
| --- | --- | --- |
| 子线归线与四件套建档 | 已完成 | `P1-J` 已正式建立 |
| `J1` keysound timing hardening | 已完成 | gameplay keysound 已切到 same-frame 播放，且 note hit / lane replay 的 owner-level timing proof 已补齐 |
| `J2` lane / ordered-hit hot path 收口 | 进行中 | `BmsLane` / `BmsOrderedHitPolicy` 的首批 `ToArray()` / 判空物化已收口，玩家命中链的重复 ordered-hit 扫描也已移除 |
| `J3` sample allocation tightening | 进行中 | `DrawableBmsHitObject` 单样本路径、shared store overload 与 channel-local 单样本双缓冲已落地；仍有 array-based sample contract 未完全消除 |
| `J4` live channel reconfigure safety | 已完成 | non-destructive resize、drawable binding focused test 与 settings tooltip 已同步 |
| `J5` focused validation / dense-chart checklist | 进行中 | ordered-hit **2/2**、shared timing **5/5**、player audio semantics **3/3**、targeted suite **11/11**、replay-focused **7/7**、autoplay combined **11/11**、autoplay-facing **4/4**、keysound-neighbour **9/9** 与完整 `osu.Game.Rulesets.Bms.Tests` **871/871** 已通过；dense-chart manual checklist 仍后置 |
| `J6` BMS→mania 转谱音频（BGM/scratch/note 走复用 store） | 进行中 | 已闭合 E（暂停停 BGM）/ 长 BGM 通道偷断（store floor 128）/ tap-note→store per-WAV cut（生产默认）/ bgm1 按键触发（BGM/scratch `Samples` 置空，2026-06-08 用户多谱实测 ✅）/ **tap-note 非池化 drawable perf（2026-06-10 改池化）** / **游玩期帧抖动（2026-06-11 同样本快路径，用户实测 ✅）** / **开局 ~220ms gen2 冻结（2026-06-11 prewarm 放开玩家模式）**；剩余转谱键音重复的 **LN 部分**（须池化嵌套头）、50k 极端 dense（仍未 profile）、BGM/scratch 非池化每帧+按键反馈重扫（次级、已记录）、128 floor、选歌预览 Track 泄漏，详见「已确认事实」与 CONSTRAINTS #7/#10/#11 |

## 当前风险

- **剩余分配风险**：shared store 已移除单样本按次数组分配，但 `SkinnableSound.Samples` 仍是 array-based contract；若后续继续下探，只能在不破坏多样本 / pooled sample 合同的前提下推进。
- **后置验收风险**：自动化回归已恢复全绿，但 dense fully-keysounded chart、layered BGM、rapid empty-strike 与 live channel change 的人工 checklist 仍未执行。
- **sample 生命周期风险**：底层 `PausableSkinnableSound` / pooled sample contract 仍没有 one-shot sample 的真 pause/resume 能力；当前已先把 pause / seek 逃逸收口为“边界即停播”，但 layered BGM 的恢复体验仍应放在后置人工 checklist 中确认。
- **主音乐验证边界**：当前新增的 player-level proof 锁住的是 `GameplayClockContainer` pause / resume 持位语义与 `BmsBgmEvent` seek 重播语义；若后续要对“带独立主音轨的 BMS 谱面在 pause 后是否按真实音频位置恢复”做更强结论，仍应补一条带实际 backing-track 观察面的专项验证或人工 checklist。
- **shared pool 边界风险**：lane replay focused scene 暴露过 pooled sample retrieval 的脆弱边界；当前 `Playfield.GetPooledSample()` 已改为在 pool 不可用时回退 `null`，由 `SkinnableSound` 继续降级到 unpooled sample。后续若再改 pooled-audio authority，必须重跑 shared timing focused suite。
- **50k 极端 dense 谱风险**：普通密度转谱谱的帧抖动与冻结已于 2026-06-11 确诊修复（同样本快路径 + 玩家模式 prewarm，用户实测 ✅），「once-per-run 卡顿」与「首次样本初始化」两条旧假设线已收口（后者被证实——冷解码确为冻结真因，且已修）。但 50k 极端 autoplay 谱当年的 `Texture upload queue large` / atlas 扩张记录提示其瓶颈可能在 update/render/present 综合预算，仍未 profile；如重提须先用 `BmsGameplayStallDiagnostics` 取证再定切法。

## 下一检查点

1. 转谱键音重复的 **LN 部分**：用已落地的 player-level harness（`TestSceneBmsToManiaKeyNoteStoreRouting` 模式 + 运行时日志）先取证，再尝试**池化嵌套头**方案让 hold 键音走 store（CONSTRAINTS #10 (a)/(b) 红线）。
2. 50k 极端 dense 谱如重新提上日程，先用 `BmsGameplayStallDiagnostics` 导出日志归因（STALL+GEN2 / 晋升风暴 / 其它），再决定切法；普通密度链路已收口，不再盲改。
3. 执行 dense fully-keysounded chart、layered BGM、rapid empty-strike 与 live channel change 的人工 checklist，并把结果回交 `P1-G`。

## 当前验证基线

- 当前 focused suite 基线已在“验证状态”和“进度矩阵”中汇总：ordered-hit **2/2**、shared timing **5/5**、player audio semantics **3/3**、targeted suite **11/11**、replay-focused **7/7**、autoplay combined **11/11**、autoplay-facing **4/4**、keysound-neighbour **9/9**，完整 `osu.Game.Rulesets.Bms.Tests` 最近快照 **871/871**（Release，2026-06-08）；mania 侧转谱：`BmsToManiaBeatmapConverterTest` **19/19**（2026-06-08，含 bgm1 修复的回归守卫「BGM/scratch `Samples` 必空、键音在 `KeysoundSample`」）、mania autoplay `TestSceneManiaModAutoplay` 覆盖转谱 store 创建 + config 读取 + prewarm 路径；BGM 通道 floor 修复后用户实测 autoplay+游玩 bgm1 人声恢复 ✅、bgm1 按键触发修复后用户多谱实测 ✅。
- 桌面端 Release 构建当前可通过；按日期展开的热路径切片、回归命令与构建记录见 [CHANGELOG.md](CHANGELOG.md)。
