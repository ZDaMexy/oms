# P1-D 技术约束：控制器校准与诊断

1. 校准与 diagnostics 必须建立在现有输入主链之上，不得复制第二套并行输入语义。
2. 不得把临时 debug overlay 冒充成正式 diagnostics UI。
3. 不得在未冻结行为边界前对外承诺具体控制器调校能力。
4. 任何改变校准范围、诊断入口或对外说明的改动，都必须同步更新本目录四件套与受影响的 `../../mainline/` 文档。