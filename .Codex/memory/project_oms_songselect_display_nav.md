---
name: project-oms-songselect-display-nav
description: P1-I 选歌展示/导航全线：I5–I7 展示层级+层级返回条+难度表分组缓存、carousel 自动跳滑修复、难度表归类/「显示转谱」三态/mania 难度表分组、BMS 模式星级→IIDX 难度等级胶囊；含落地映射+红线踩坑
metadata: 
  node_type: memory
  type: project
  originSessionId: 94adfdcc-cd0f-41d7-8106-5e73689a6d4d
---

P1-I 新增主题 I5–I7（区别于 I0–I4 的筛选/搜索/read-model）。**已于 2026-06-16 落地、2026-06-18 经用户实机反馈再修两轮，用户实机验收通过**（focused 回归全过、Release 编译 0 错误）。四件套（P1-I PLAN/STATUS/CHANGELOG「其二」+「2026-06-18」+「2026-06-21」/TECHNICAL_CONSTRAINTS「展示层级与层级导航约束」#1–18）+ P1-A 从属指针 + mainline CHANGELOG/STATUS 已同步（见 [[project_oms_docs_governance]]）。

**落地映射**：I5=`FilterControl` 的 `displayLevelDropdown`(共享 sort/group/collection 行的 BMS-only 第 4 列，`createSortGroupColumns(showDisplayLevel)` 在 `updateRulesetSpecificFilters` 切列宽、非 BMS 收 0)/`displayLevelSetting`(`OsuSetting.BmsSongSelectDisplayLevel`)/`displayLevelForcedStandalone`/`updateDisplayLevelDropdownState` + `FilterCriteria.DisplayLevel`(nullable) + `BeatmapCarouselFilterGrouping.ShouldGroupBeatmapsTogether`/`GroupingForcesStandaloneDifficulties` + enum `Screens/Select/Filter/DisplayLevel.cs`；I6=`BeatmapCarousel.CurrentExpandedGroup`/`CollapseExpandedGroupOneLevel`(cursor 停在刚折叠的组、不重新展开) + `ISongSelect` 两成员 + `FilterControl.GroupNavigationDisplay.cs`(只显示面包屑、无前缀)；I7=`BmsTableGroupMode` 的 `group_cache`(键=RulesetDataJson)/`ComputeGroupDefinitions`；层级视觉区分=`PanelGroup.PrepareForUse` 按 `group.Depth` 高对比多线索(根=Background4+三角纹理+Heading2+Content1 / 嵌套=Background6+`triangles.Alpha=0`纯平+Body SemiBold+Content2+Depth*24 缩进)——**相邻 Background shade(5↔6)实机不可辨，必须亮度跨档+纹理有无+字号+字色+缩进多线索叠加**。**红线踩坑**：禁用 bindable 前必须先清 `Disabled=false` 再改 `Value`（否则抛）；display-level 偏好与下拉显示值要分离避免锁定期污染 config；`PanelXOffset` 是 init-only、池化面板不可逐项改，故层级区分用 shade+字号 + 运行期 `Panel.AdditionalXOffset`(虚方法)而非 PanelXOffset。

**2026-06-18 修正（非 BMS 播放谱面进入 BMS 误展开分组，已落地+测试）**：启动自动随机播放的曲目若是 mania，进入/切到 BMS 难度表分组总误展开某组（用户见 Unrated）。根因=`SongSelect.ensureGlobalBeatmapValid` 的 `shouldSuppressGroupedAutoSelection()`（fresh-entry root-focus 抑制自动选中）**只在 `if(validSelection)` 分支内检查**；mania 当前谱面对 BMS invalid → 走 invalid 回退 `SetDefault→IsDefault→NextRandom` 自动选中+`setExpandedGroup`，而 `FocusRootGroupForBeatmap` 聚焦不到 mania（不在 BMS carousel）→ `pendingRootGroupFocus` 恒真、抑制被回退绕过。修复=把抑制**提前到 valid/invalid 分支之前**短路返回。只影响 `ShouldResetSongSelectGroupToRoot` 为真的 ruleset(BMS)；mania `pendingRootGroupFocus` 永不置真、零影响。回归 `TestNonBmsPlayingBeatmapDoesNotExpandGroupOnEntry`(去修复即失败已验证)。918/918。

**2026-06-18 实机修正（层级展开态/缩进，已落地+测试）**：① 层级分组下**路径根组的 `IsExpanded` 结构性永不被置位**——`setExpansionStateOfGroup` 只设子组的 IsExpanded（由父组驱动），根组无父组故被漏掉 → 展开的表名层无 chevron + 吃"未展开"右推偏移。修复=`setExpandedGroup` 用 `setGroupItemExpansion(path.First(), bool)` 经 `grouping.ItemMap` 显式置/复位根组。② `Panel.updateXOffset` 突出量不看深度 → 键盘选中的子组比父组更突出（反直觉）。修复=新增 `Panel.AdditionalXOffset`(virtual，默认0，updateXOffset 叠加)，`PanelGroup` override=`Depth*30`，**30 必须 > `active_x_offset`(25)** 才能压过键盘选中偏移、保证祖先突出≥后代。映射:`BeatmapCarousel.setGroupItemExpansion`/`Panel.AdditionalXOffset`/`PanelGroup.depth_x_offset`。回归 `TestExpandedTableHeaderSharesExpandedStateWithLevel`。BMS 全套 917/917。

**2026-06-21 修正（选歌两处「自动跳滑」，已落地+测试，用户实机验收通过）**：均在「mania 曲目正在播放时进入 BMS 难度表分组」场景，机制不同。**Bug1 选谱 root-jump**：进 BMS 后手动选任一谱 → 画面滑到该谱最外层（表名）组。根因＝`pendingRootGroupFocus` 在当前全局谱面对 BMS invalid（mania）时 `FocusRootGroupForBeatmap` 恒 false、标记长挂，等用户选第一张 BMS 谱、全局 `Beatmap` 变更触发 `updateVariousState→tryFocusRootGroupForCurrentBeatmap` 时被劫持滚到 root。修复＝`tryFocusRootGroupForCurrentBeatmap` 在 `carousel.CurrentGroupedBeatmap != null` 时清标记+早退（与 [[project-oms-songselect-display-nav]] 自身 2026-06-18 #16「抑制 invalid 期间自动选中」对称：#16 抑制自动选中，#17 放弃永不满足的延迟聚焦，同一 `pendingRootGroupFocus` 生命周期两收口点、改一须复核另一）。**Bug2 窗口还原 group-jump**：选歌列表自由滚走后按 Win 最小化再返回 → 跳到当前展开层级组头。根因＝最小化/还原改 carousel `DrawSize` → base `Carousel.OnInvalidate` 无条件 `selectionValid.Invalidate()`→`ScrollToSelection`，而无具体选中时 `BeatmapCarousel.GetScrollTarget` 回退到键盘光标/`ExpandedGroup` 位置、拽回组头。修复＝base `Carousel.OnInvalidate` 的 DrawSize 分支限定 `currentSelection.CarouselItem != null` 才 re-center。**mania 安全**＝其选歌恒有已提交选中（`ShouldActivateOnKeyboardSelection` 使方向键浏览即 `Activate` 提交→`currentSelection.CarouselItem` 非空），零行为变化；「键盘光标可停组头不提交选中」是 BMS 层级分组特性、本 bug 成因边界（故不能读 `currentKeyboardSelection`——它可能就是组头）。改动＝`SongSelect.cs`(bug1)+shared `Carousel.cs`(bug2)。约束 #17/#18。回归 `TestSelectingChartWhileNonBmsPlayingDoesNotJumpToRootGroup`（BMS 套件）+ `TestDrawSizeChangeDoesNotRecentreWithoutSelection`+守护 `TestDrawSizeChangeRecentresCommittedSelection`（osu.Game.Tests generic carousel；用 `Carousel.Width=` 触发 DrawSize 失效，去修复分别验证失败：root 跳 "Satellite" / 偏移 ~281px）。BMS 分组 11/11+新增 3 全过；osu.Game.Tests carousel 18 条 failure 经 `git stash` baseline 比对＝**pre-existing headless flaky**（输入驱动/排序稳定性，与本改动无关）。

**2026-06-22（标准面板第 4 排加难度表归类，已落地+测试）**：展示层级＝「谱面」时 `PanelBeatmapStandalone` 第 4 排由「星级 + SpreadDisplay 按钮」改为「星级 + **难度表难度归类** + 按钮」，BMS 选曲与转谱-mania 都生效；多难度表索引按 `TableSortOrder` 顺序 `/` 分隔列举（Satellite-sl4→`sl4`；+发狂★8→`★8/sl4`）。链路：osu.Game 不引用 BMS ruleset → 新增 `BmsPersistedMetadataResolver.GetDifficultyTableEntries`（osu.Game）**只读** `BmsPersistedMetadataData.ExtensionData["difficulty_table_entries"]`（osu.Game 侧 `BmsPersistedDifficultyTableEntry` 只建模 `TableName/LevelLabel/Level/TableSortOrder`、**绝不回写**——红线：**禁建模成可写 DTO 字段**，否则 converted-star 写回抹掉 `Symbol`/`Md5` 重蹈 [[reference-bms-difficulty-table]] 共享 RulesetData 列 clobber ping-pong）；展示收口 `BeatmapLocalMetadataDisplayResolver.GetDisplayDifficultyTableClassification`（display-only，键于 `Ruleset.ShortName=="bms"`，故 BMS-mode 与转谱-mania 同谱取值一致）= 按 TableSortOrder 升序、每表取一 `LevelLabel`（已含符号前缀 `★8`/`sl4`）、`/` 连接。面板：第 4 排 FillFlow 在 star 与 spread 间插 `difficultyTableText`（Caption1 SemiBold + Content1），`PrepareForUse` 取值、空（非 BMS/Unrated）则 `Alpha=0` 借「FillFlow 非 present 子项不排布」消除星级↔按钮幽灵间距；只在 PrepareForUse 取一次（归类不随 ruleset/mod 变；中途改难度表仍沿用既有重进选歌/重启生效边界）。表顺序＝当前 `TableSortOrder`（导入/启用序），日后难度表管理改造改序自动跟随。回归 `BeatmapLocalMetadataDisplayResolverTest` 4 条（单表/多表 sort order/Unrated 空/非 BMS 空）7/7、osu.Game Release 0 错误。约束 #19。**用户 2026-06-22 实机验收通过（暂无异常）。**

**2026-06-22（其三：mania「显示转谱」改三态，已落地+测试）**：mania 选曲筛选区「显示转谱」从 bool 二态升三态 **禁用/启用/仅显示转谱**（仅转谱＝隐原生 mania、只留 BMS 转谱）。**单一 enum 收口**＝新 `ConvertedBeatmapsDisplay { Hidden, Shown, ConvertedOnly }`（`osu.Game/Configuration/`，`[LocalisableDescription]`→`OmsSongSelectStrings`+resx/zh.resx 三串）；`OsuSetting.ShowConvertedBeatmaps`(bool)**改名** `ConvertedBeatmapsDisplay`(enum，默认 Shown，旧键作废=重置默认)。`FilterCriteria` 加 `ConvertedBeatmaps` enum + `AllowConvertedBeatmaps` 降为 **bool 投影属性**（get `!=Hidden`/set 映射 Shown·Hidden——保留可设以零改现有测试 + 给 star-lookup 门稳定 bool）。过滤收口＝`BeatmapCarouselFilterMatching` 一处 switch（Hidden=`allowConversion:false`/Shown=`true`/ConvertedOnly=`Ruleset!=当前 && true`）。UI＝新 `ConvertedBeatmapsDisplayButton`(继承 ShearedButton，循环 Hidden→Shown→ConvertedOnly，文案+色随态)绑 mania 标准布局；BMS 布局保留二态 ShearedToggleButton + **带 echo 守卫双向同步**(`if(e.NewValue==convertsAllowed)return` 断环)；设置面板 `FormCheckBox→FormEnumDropdown`。**BMS 夹紧红线**＝OMS 仅 BMS→mania 一条转谱路，`仅转谱` 对非 mania 会清空列表 → `FilterControl.CreateCriteria` 把非 mania 的 ConvertedOnly 夹回 Shown(`mania_ruleset_short_name`)，BMS 按钮也只出 Hidden/Shown。派生消费方(SongSelect×2/OsuGame/两 SpreadDisplay/NoResults)全改读 `enum!=Hidden`。回归 `FilterMatchingTest` +4(98/98)+carousel sort·group 25/25，slnf Release 0 错误。约束 #20。**用户 2026-06-22 实机验收通过（暂无异常）。**

**2026-06-22（其四：mania 新增「难度表」分组，已落地+测试）**：mania 选曲分组下拉加「难度表」（行为同 BMS 难度表分组），但**只显示 BMS 转谱**、排除原生 mania；空结果给指导。**跨 ruleset 取数**＝mania 不引用 BMS ruleset → 新增 osu.Game 共享 `BmsConvertedDifficultyTableGrouping`(`Screens/Select/`)，用 `BmsPersistedMetadataResolver.GetDifficultyTableEntries`(只读 ExtensionData，见其二) 构 表名→等级 树(无条目 Unrated)+RulesetDataJson 内容键缓存，镜像 `BmsTableGroupMode`。**「只显示转谱」关键＝grouping 丢弃法、零改 matching**：`addHierarchicalGroups` 丢弃**返回空 group 定义**的谱面 + `MatchedBeatmapsCount=Filters.Last()`(grouping 阶段)，故 helper 对**非 BMS**(`Ruleset.ShortName!="bms"`)返回空 → 原生 mania 被丢、计数与列表一致；**不要 force ConvertedOnly/改 matching**(会让计数与列表打架)。转谱可见性仍由 #20 `ConvertedBeatmaps` 设置在 matching 把关(Hidden→空)。`ManiaRuleset` override 4 虚方法接线(GetAvailableSongSelectGroupModes 插 DifficultyTable / IsSongSelectGroupingHierarchical / ShouldResetSongSelectGroupToRoot / GetSongSelectGroupDefinitions→helper)，复用既有 `GroupMode.DifficultyTable`。`NoResultsPlaceholder` 空结果指导(Hidden→可点启用转谱 / 已开但空→提示导入 BMS，抑制通用 convert hint)。3 串 OmsSongSelectStrings+resx/zh。回归 `BmsConvertedDifficultyTableGroupingTest`(4)+`ManiaDifficultyTableGroupingTest`(3 端到端丢原生)+`BeatmapCarouselFilterGroupingTest` 17/17 不回归，slnf Release 0 错误。约束 #21。**用户 2026-06-22 实机验收通过（暂无异常）。**

**2026-06-22（其五：BMS 模式选曲星级→IIDX 难度等级胶囊，已落地+测试，实机验收待确认）**：仅「当前 ruleset=bms 且 beatmap=bms」时，选曲星级胶囊换成 IIDX 难度等级胶囊——保留原圆角背景，显示「标签 等级」（如 `NORMAL 7`）、去小数、按 `#DIFFICULTY` 上色。**门控严格双条件**，转谱-mania 视图（mania 模式下 BMS 谱面）**必须保留真实转谱星级**（转谱星有意义）。**对齐结论（用户选定）**：未定义难度＝`UNKNOWN ＋等级`；等级用**原始 `#PLAYLEVEL` 文本**（verbatim，不解析数值，`12+`/`★7` 原样）；小星星 starCounter / 分布点 SpreadDisplay 等其它星形元素**全部保持现状**。**数据源零碰解析层/零重导**：`#DIFFICULTY`→HeaderDifficulty、`#PLAYLEVEL`→PlayLevel 导入即随 chart metadata 持久化、`BmsPersistedMetadataResolver.GetChartMetadata` 直读（`GetDisplayDifficultyName` 早已在用 HeaderDifficulty）。**落地映射**：文案 `BeatmapLocalMetadataDisplayResolver.GetDisplayDifficultyLevel`（大写 IIDX 标签 UNKNOWN/BEGINNER/NORMAL/HYPER/ANOTHER/INSANE + 原始 PlayLevel；无 playlevel 仅标签；新 helper `getBmsIidxDifficultyLabel`——**区别于** title-case 且未定义返回 null 的 `getBmsHeaderDifficultyLabel`，胶囊永远要有标签）+ `GetBmsDifficultyTier`（1–5 或 0）；配色 `OsuColour.ForBmsDifficultyLevel(tier)`＝白/绿`#2fb24c`/蓝`#2e8be6`/黄`#f4c20d`/红`#e53935`/紫`#9c44d6` + `ForBmsDifficultyLevelText`(0/3 深字、余白字)；新组件 `BmsDifficultyLevelDisplay`(`Beatmaps/Drawables/`，镜像 StarRatingDisplay 圆角形去星标无动画，暴露 `DisplayedDifficultyColour/TextColour`)。**挂载红线**＝三处(`PanelBeatmap`/`PanelBeatmapStandalone`/`BeatmapTitleWedge.DifficultyDisplay`)把新胶囊与原 `StarRatingDisplay` 并存、按门控 alpha 0/1 互斥(alpha 0 被 FillFlow/AutoSize 视为非 present 不占位)；**原星级胶囊保持存活(alpha 0)继续被 `difficultyCache.GetBindableDifficulty` 喂色** → 面板 tint/三角/starCounter/SpreadDisplay(含 current 点)全部保持现状(光谱色)，**不动任何既有 Update() 强调色链路**；wedge 难度名取色 BMS 时改用 `bmsLevelDisplay.DisplayedDifficultyColour`(IIDX)。display-only，不动存库 DifficultyName/StarRating/MD5。回归 `BmsLocalMetadataDisplayResolverTest` 14/14(+5)，slnf Release 0 错误。约束 #22。**选曲三处用户已实机确认「符合预期」。** **同日追加：加载界面 `PlayerLoader` 的 `BeatmapMetadataDisplay`**——点开始后的加载页此前难度名仍显存库 `Another 11`、星级仍旧 pill，与选曲不一致。该组件**原本已对 artist/creator 走 resolver**，本次补齐：标题 `GetDisplayTitle/Unicode`(剥 BMS 标题尾括号，避免标题 `[SP ANOTHER]` 与难度名 `SP ANOTHER` 重复)、难度名 `GetDisplayDifficultyName`、星级换 `BmsDifficultyLevelDisplay`(门控＝play ruleset[`[Resolved] IBindable<RulesetInfo>`＝OsuGameBase 全局 `[Cached(typeof(IBindable<RulesetInfo>))] Ruleset`]＝bms 且 beatmap＝bms；转谱-mania 保留真实星级——`GetBindableDifficulty(beatmapInfo)` 无 ruleset 参本就用 cache `currentRuleset`＝全局 play ruleset，与门控天然一致)。**红线：只改 `BeatmapMetadataDisplay`，不动中央 `BeatmapInfoExtensions.GetDisplayTitleRomanisable`**(仍服务 now-playing/results、出本专题范围——见 [[reference-bms-songselect-metadata-display]])。`BeatmapMetadataDisplay` 是视觉组件无 headless 单测、复用同组 resolver。加载界面实机待确认。

**2026-06-23（carousel 面板两处音符图标，已落地，用户实机暂未见异常）**：两项独立 UI 调整。**① 曲名右侧加 preview 指示音符**（仅 BMS 模式 + BMS 谱 + 有试听）：`PanelBeatmapStandalone` 把 `titleText` 包进水平 FillFlow，右侧加 `previewIcon`（`SpriteIcon` FontAwesome.Solid.Music，Size10，Content2，默认 Alpha0），在 `updateDifficultyLevelDisplay` 里 `previewIcon.Alpha = useBmsLevel && !IsNullOrEmpty(beatmap.Metadata.AudioFile) ? 1 : 0`（`useBmsLevel`＝当前 ruleset=bms 且 beatmap=bms；非空 AudioFile＝有 #PREVIEW＝会试听，与实际试听同一信号）。只在 standalone（有曲名处），grouped `PanelBeatmap` 无曲名不加。**② 删最左 lamp 上的 ruleset 音符图标（BMS，保留 lamp 块）**：`BmsRuleset.CreateIcon()`＝FontAwesome.Solid.Music，即左侧块上的音符。`PanelBeatmap`+`PanelBeatmapStandalone` 的 `PrepareForUse` 加 `difficultyIcon.Alpha = beatmap.Ruleset.ShortName=="bms" ? 0 : 1`，**且创建 `difficultyIcon` 时 `AlwaysPresent = true`**。**★坑（首版把 lamp 也删了，用户截图发现）**：base `Panel.iconContainer` 是 **AutoSize**、`contentPaddingContainer.Padding.Left = iconContainer.DrawWidth`；AutoSize **不计入 Alpha=0 的非 present child**（与 bmsLevelDisplay alpha 互斥同一机制）→ 只设 Alpha=0 → iconContainer 收缩到 0 → 左 padding 0 → beatmap 背景盖住 lamp 颜色块（`backgroundBorder`），**lamp 连块一起没**。`AlwaysPresent=true` 让隐藏 icon 仍占布局宽 → lamp 块保留、标题不位移、仅音符不可见（＝lamp 状态去掉音符）。**教训：AutoSize 里"只设 Alpha=0 保留占位"是错的——会被当非 present 排除，要保留占位必须 AlwaysPresent=true。** 两改皆 core osu.Game；panel 无 headless 单测（`TestScenePanelBeatmap*` 在 osu.Game.Tests `<Compile Remove>`），靠实机看。slnf Release 0 错误。preview 指示音符用户实机确认正常；删音符保留 lamp 经 AlwaysPresent 修正后用户 2026-06-23 实机暂未见异常。另：backfill 进度通知文案「选歌试听」→「选歌预览」（[[reference_bms_gameplay_track_mute]]）。

**现状真相（动手前必读）**：当前"歌曲→谱面折叠"vs"谱面扁平"由 `BeatmapCarouselFilterGrouping.ShouldGroupBeatmapsTogether` 的隐式 Sort/Group 组合决定（BMS 层级分组/Group=Difficulty/Sort=Difficulty 等 → 扁平；其余 → 折叠），无用户控制、反直觉。"临时展示这张谱面下的所有难度" = `PanelBeatmapStandalone.SpreadDisplay` → `ISongSelect.ScopeToBeatmapSet` → `scopedBeatmapSet` bindable → `FilterControl.ScopedBeatmapSetDisplay` 顶部 banner（带 返回 + Back 键）。难度表分组链 = `BmsRuleset.GetSongSelectGroupDefinitions(DifficultyTable)` → `BmsTableGroupMode`（两级树：表名→等级，无条目→Unrated）→ `IsSongSelectGroupingHierarchical=true` → `addHierarchicalGroups`。退层原语已齐：`setExpandedGroup`/`ExpandedGroup`/`ResetToRootLevel`/`FocusRootGroupForBeatmap`，点已展开组头即折叠回父（`HandleItemActivated`）。

**三项设计（已选档）**：
- I5 展示层级：BMS-only `ShearedDropdown`，两档「歌曲→谱面」/「谱面」，经新增 `FilterCriteria.DisplayLevel`(nullable) 驱动 `BeatmapSetsGroupedTogether`（null=mania 走旧启发式零变化）；层级分组/Group=Difficulty 锁定为「谱面」并禁用（复用 sort-lock 的 `sortModeBeforeLockedGrouping` 还原模式）；新 BMS-local `OsuSetting` 持久化。
- I6 层级返回条：仿 `ScopedBeatmapSetDisplay` 但**由 `ExpandedGroup` 驱动、严禁复用 `ScopedBeatmapSet` 状态**（两语义不同：scope=绕筛选展开 set；退层=折叠分组路径）。carousel 暴露只读 `CurrentExpandedGroup`，`ISongSelect` 加折叠一级动作。Back 优先级：scoped-set > 层级退回 > 退出 song-select。顺手把 `TemporarilyShowingAllBeatmapsIn` BMS 文案"谱面"消歧为"歌曲/谱面集"(display-only)。
- I7 分组性能：`BmsTableGroupMode` 每 refilter×每谱调 `GetDifficultyTableEntries()` = `BeatmapMetadata.GetRulesetData<T>` 全量 `JsonConvert.DeserializeObject`(BeatmapMetadata.cs:86)，5万+库即每操作 5万+ 反序列化。**实现时直接按 RulesetDataJson 内容键加了有界缓存（correctness-neutral、纯函数+immutable record、测试锁定），未走"先实测再加"**（参见 [[reference_song_select_perf]] #2）；大库实际收益量化仍建议用户实机确认。

红线：单一收口点 `BeatmapSetsGroupedTogether`；不新开 per-ruleset FilterControl host；不在过滤阶段逐张 GetWorkingBeatmap（[[reference_bms_composition_filter]] read-model #13）。

**2026-06-18 选歌偶发掉帧观察（未定位 / 已搁置）**：测 I5–I7 分组/展示层级行为时偶发 ~1000→~25FPS 持续波动、切分组层级不恢复、事后不可复现。逐文件审查 I5–I7 全链（展示层级下拉 / 面包屑导航 / `PanelGroup` 深度渲染 / `BmsTableGroupMode` 分组缓存 / `Panel.AdditionalXOffset`）= **事件驱动**（仅 group/level 变更执行）、filter/grouping 走后台线程，**未定位为成因——下次别再先怀疑分组代码**（"切分组层级不恢复"恰反证不是这条只在变更时跑的管线）。用户所给日志**不含任何帧率/帧时长/线程耗时**，唯一 Global Statistics 快照在 SettingsOverlay 打开瞬间（纹理上传队列 1200、TextureAtlas 扩 7 页），非掉帧现场，证据不足以定因。捕获协议：`Ctrl+F11` 看 Update/Draw/GC 哪条标红 + Alt-Tab 切走切回是否回升（验"窗口非活动→帧率限制器"，2 秒可证伪）+ 当场重导日志拿掉帧期间快照。候选（按吻合度）：①纹理图集耗尽走非图集 → Draw 绑定暴增（既有大库特性、最吻合 Draw-bound+不恢复，见 [[reference_song_select_perf]]）②窗口判非活动 → 帧率限制器（吻合切分组无效）③GC/池泄漏（无证据）。详见 P1-I CHANGELOG 2026-06-18 其四。
